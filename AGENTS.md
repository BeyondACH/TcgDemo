# AGENTS.md

本文档用于帮助协作代理快速理解并安全修改本仓库。

## 项目概览

- 引擎：Godot 4.6
- 语言：GDScript
- 入口场景：`res://scenes/battle_scene.tscn`
- 项目类型：本地 1v1 卡牌对战原型，规则设计参考 UNION ARENA 风格

## 目录说明

- `core/`：回合流程、规则结算、战斗处理、胜负判断、区域管理
- `data/`：运行时数据模型，以及卡牌和卡组等静态数据
- `scenes/`：Godot 场景文件
- `ui/`：战斗界面、手牌区、场地区、卡牌表现、日志和阶段提示
- `rule.md`：玩法规则和程序设计参考文档

## 开发约定

- 规则逻辑尽量放在 `core/`，状态数据尽量放在 `data/`。
- 优先做小而明确的修改，避免无必要的大范围重构。
- 除非收益明确，否则保持现有命名方式和脚本组织结构不变。
- 修改玩法行为时，同时检查状态流转和 UI 表现是否受影响。
- 只有在实现需求确实需要时，才更新或新增示例数据。

## Godot 相关注意事项

- 除非任务明确要求，否则不要手动编辑 `.uid` 文件。
- 编辑 `project.godot` 时保持改动尽量小，并确保目的明确。
- 始终保持 `res://` 路径和场景、脚本引用有效。
- 优先使用便于 git 审查的纯文本修改方式。

## 验证清单

- 重命名脚本或资源后，检查是否产生失效引用。
- 如果改动涉及回合、战斗或效果结算，顺带检查相关 manager 和 resolver。
- 如果改动 UI 脚本，确认它仍与对应场景节点结构匹配。

## 编码与文本

- 编辑 Markdown、JSON 和 GDScript 文件时统一使用 UTF-8。
- 如果终端里出现中文乱码，先确认文件编码，再决定是否重写内容。

## 扩展项目时的建议

- 新的通用玩法系统优先放入 `core/`，前提是它属于可复用规则逻辑。
- 新的共享状态或数据结构放入 `data/`。
- 只和具体界面表现相关的逻辑保留在 `ui/`。
- 如果某个系统行为不直观，请在相关代码旁或 `rule.md` 中补充说明。

## 项目里程碑

### 当前已实现

- 已完成基础对局初始化：加载卡牌定义、加载双方卡组、洗牌、起手抽牌、设置生命区。
- 已完成基础回合流程：开始阶段、移动阶段、主要阶段、攻击阶段、结束阶段，以及主动玩家轮换。
- 已完成 AP 成长与消耗机制：首回合/后续回合 AP 增长、出牌时扣除 AP、每回合重置可用状态。
- 已完成基础区域管理：牌库、手牌、生命区、前线、能量区、场外区、移除区之间的移动。
- 已完成三类基础卡牌落地规则：角色牌、场地牌、事件牌的出牌校验与区域限制。
- 已完成能量需求校验：根据能量区提供的颜色判断是否满足出牌条件。
- 已完成基础移动规则：移动阶段可将能量区中的角色移动到前线。
- 已完成最小可用战斗流程：宣言攻击、可选阻挡、BP 比较、未被阻挡时对玩家造成伤害。
- 已完成基础胜负判断：生命归零败北、空牌库抽牌失败败北。
- 已完成基础效果结算器：支持 `DRAW`、`MOVE_ZONE`、`REST`、`ACTIVATE`、`DEAL_DAMAGE_TO_PLAYER`。
- 已完成基础触发机制：支持入场、攻击、阻挡、生命触发等触发入口。
- 已完成可操作 UI 原型：战场展示、手牌展示、选牌、按钮出牌、拖放到前线/能量区、攻击与阻挡选择、日志显示。
- 已接入示例数据：基础角色、场地、事件以及 starter 卡组，可用于跑通最小对局。

### 待实现

- 完整规则细化仍未完成：当前更接近“最小可玩原型”，距离完整 UNION ARENA 规则还有明显差距。
- 更丰富的卡牌效果系统尚未实现：检索、指定目标、持续效果、数值修正、条件判定、连锁/时点响应等能力仍需补齐。
- `MAIN_ACTIVATE` 等主动能力还没有真正落地到交互和结算流程。
- `ON_LEAVE` 等离场触发目前没有统一从区域变更流程里自动派发，离场类卡效支持还不完整。
- 战斗规则仍较简化：先攻/后攻细则、更多攻击限制、复杂阻挡规则、特殊关键字处理尚未实现。
- 关键字系统基本未实现：虽然数据里出现了如 `RAID` 的字段，但代码层面还没有对应规则支持。
- 真实卡牌数据兼容性不足：当前示例 JSON 中已经出现编码异常和复杂效果文本，尚未建立稳定的数据清洗与导入流程。
- UI 仍是原型界面：缺少更清晰的状态提示、目标选择交互、动画反馈、错误提示优化和移动端/缩放适配。
- 缺少 AI、联网或本地双人控制切换等更完整的对战模式能力。
- 缺少自动化测试：核心规则、回合流转、效果结算、胜负判定都还没有测试覆盖。
- 缺少存档、对局回放、调试面板、规则断言等开发辅助能力。

### 建议中的下一阶段

- 里程碑 1：补齐稳定的卡牌数据格式与编码清理流程，确保卡牌定义可持续扩展。
- 里程碑 2：扩展效果系统，优先支持目标选择、数值变更、离场触发和主要阶段主动能力。
- 里程碑 3：完善战斗和关键字规则，让原型从“能跑”提升到“规则可信”。
- 里程碑 4：补测试与调试工具，降低后续继续加卡和加规则时的回归风险。
- 里程碑 5：打磨 UI/UX，再考虑 AI、联网或更多对战模式。
