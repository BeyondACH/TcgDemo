

本文档是本仓库的多 agent 协作与交付规范。目标不是重复项目说明，而是让主 agent、worker、reviewer 在同一轮开发中可以稳定拆分、清晰交接、统一验收，并始终保持实现与规则文档一致。

---

## 1. 文档目标与权威来源

### 1.1 项目事实

- 引擎：Godot 4.6
- 语言：GDScript
- 入口场景：`res://scenes/battle_scene.tscn`
- 项目类型：本地 1v1 卡牌对战原型，规则设计参考 UNION ARENA 风格

### 1.2 权威来源

- `rule.md` 是本项目规则、状态机、区域限制、结算顺序和数据映射的最高权威来源。
- 任何代码实现、测试、重构、示例数据、注释说明，只要与 `rule.md` 冲突，都必须以 `rule.md` 为准。
- 如果用户需求与 `rule.md` 冲突，不要直接实现冲突方案；先指出冲突点，再给出遵循 `rule.md` 的实现建议。

### 1.3 开发计划文件

- 项目正式开发计划保存在 `docs/plan/development_plan.md`。
- 主 agent 在进入需求拆分、接口冻结、并行分工、阶段验收前，应先对齐 `docs/plan/development_plan.md` 与 `docs/rules/rule.md`。
- 如果新的需求会改变里程碑优先级、接口冻结项、测试策略、DSL/IR 契约或阶段主线，应先更新 `docs/plan/development_plan.md`，再组织实施。

### 1.4 当前目录职责

- `core/`：回合流程、规则校验、战斗、效果结算、胜负判断、区域管理
- `data/`：运行时状态模型、卡牌定义、卡组数据
- `ui/`：界面层、交互脚本、快照消费
- `scenes/`：Godot 场景
- `docs/`：测试脚本、说明文档、辅助资料

---

## 2. 使用说明

本规范分为两类：

- **硬性规则**：违反即视为未完成
- **推荐做法**：默认遵循，特殊情况可说明例外

主 agent 在开始实现前，至少应先阅读：

- 最小执行清单
- 硬性规则
- 与本轮任务相关的边界、验收与异常处理章节

worker 至少应阅读：

- 分配给自己的任务单
- 自身写入范围相关的边界规则
- 交付模板与验证要求

---

## 3. 最小执行清单

### 3.1 开工前清单

- 是否已对齐 `rule.md`
- 是否已确认本轮适合串行还是并行
- 是否已识别高风险规则区
- 是否已锁定写入边界
- 是否需要先冻结接口、数据契约或 DSL/IR 契约
- 是否已确认依赖模块的消费方式
- 是否已检查最近相关日志，优先查看当日日志

### 3.2 提交前清单

- 是否未越界修改未授权目录或文件
- 是否补了至少一种验证
- 是否复核接口、信号、数据结构影响
- 是否同步更新日志、计划、README
- 若涉及中文文档，是否完成显式 UTF-8 校验
- 若涉及 UI / 布局，是否确认手牌区域未遮挡战场区域
- 若涉及高风险规则区，是否完成流程级或最小对局级验证

---

## 4. 硬性规则

### 4.1 规则与实现一致性

- `rule.md` 优先级最高。
- 不允许为了实现方便而改变 `rule.md` 语义。
- 不允许用户需求直接覆盖 `rule.md`；若冲突，必须先指出冲突点。

### 4.2 并行开发边界

- 不允许两个 agent 同时修改同一个 `.gd` 文件。
- 不允许一个 agent 修改 `core/` 公共接口，另一个 agent 同时基于旧接口改 `ui/`。
- 不允许未冻结数据契约前，同时修改 `data/` 与多个依赖该结构的 `core/` 子模块。
- 不允许一边改规则语义，一边让别的 agent 基于旧语义补测试。
- 不允许把 UI 层逻辑反向塞入规则核心。

### 4.3 高风险规则区

以下区域默认视为高风险规则区：

- 生命区触发
- 攻击 / 阻挡结算
- 区域容量上限
- AP 与能量支付
- 回合切换与状态恢复
- 胜负判定
- 卡牌 DSL/IR 契约
- `EffectResolver` 运行时效果模型

凡触及高风险规则区时：

- 必须由主 agent 定案
- 默认串行开发
- 必须补至少一种流程级验证
- 不得在旧规则语义上继续补实现或补测试
- 如影响规则口径、计划主线或统计基线，必须同步更新文档

### 4.4 不可破坏的规则约束

- 前线最多 4 张角色
- 能量线最多 4 张卡
- AP 区域最多 3 槽
- 主卡组 50 张
- 起手 7 张
- 生命区初始 7 张
- 只有 ACTIVE 的前线角色可以攻击或阻挡
- 场地牌只能进入能量线，不能进入前线
- 胜负判断至少覆盖生命归零与空牌库抽牌失败

### 4.5 文档与日志同步

- 每次功能更新与 bugfix，都必须在同一轮改动中同步更新：
  - `docs/plan/development_plan.md`
  - `docs/plan/mile_stone.md`
  - `README.md`
  - `docs/logs/log_yyyy-MM-dd.md`
- 如果代码已更新，但计划、README、日志仍保留旧口径，则该次工作视为未完成。
- 日志内容必须使用中文。

### 4.6 中文文档与编码安全

- 仓库中的代码文件与中文文档统一使用 UTF-8 编码。
- 涉及规则、计划、日志、中文 UI 文案、断言信息等关键中文片段时，不得直接依赖终端默认输出做结论。
- 修改中文文档时，不得把中文正文直接内联到提权命令、跨 shell 命令链、here-string、here-doc 或复杂命令字符串中再写回文件。
- 中文文档修改后，必须立即执行显式 UTF-8 重读校验，并结合 `git diff` 静态复核。
- 一旦发现乱码已落盘，必须先停止继续编辑，先确认污染范围，再做最小修复。

---

## 5. 推荐协作方式

### 5.1 角色定义

#### 主 agent

- 负责理解需求、制定拆分方案、分配写入边界、整合结果、最终验收
- 负责所有跨模块决策，尤其是规则语义、接口契约、状态流转、冲突裁决
- 负责决定哪些任务可以并行，哪些必须串行

#### worker agent

- 只实现被分配的明确范围
- 不自行扩大修改面
- 不擅自重构公共接口
- 遇到跨模块依赖时，只报告阻塞、假设和建议，不自行拍板

#### reviewer / checker agent

- 只做验证、回归检查、风险枚举和验收建议
- 默认不与实现 agent 修改同一写入面

### 5.2 基本协作原则

- 先锁边界，再并行开发
- 先冻结接口，再让依赖方接入
- 一旦涉及 `core + data + ui` 跨层联动，默认先由主 agent 锁接口
- 用户显式要求使用 `subagent` skill 时，默认视为主 agent 已获准采用多 agent 并行模式，但仍必须先锁边界、接口契约与风险区

### 5.3 推荐并行场景

- 已锁接口前提下的 UI 适配
- 文档补充与规则说明整理
- 冒烟测试、回归测试、样例数据补齐
- 相互独立的 `core/` 子系统小改动
- DSL/IR 契约已冻结前提下的数据补录、样例卡编写与 UI 消费适配

### 5.4 推荐串行场景

- 修改回合状态机
- 修改攻击 / 阻挡结算顺序
- 修改触发系统和效果执行模型
- 修改卡牌 DSL/IR 契约、原子步骤集合或导入到运行时 IR 的编译链路
- 修改 `GameManager` 快照结构且多个模块都依赖它
- 大范围数据结构迁移

---

## 6. 并行分工边界

### 6.1 可并行工作面

#### `core/`

- 高风险区
- 同一轮并行中，默认只允许 1 个 agent 修改同一个规则子系统
- 可按 `turn / battle / effect / zone / victory` 子模块拆分
- 前提是主 agent 已锁定接口和状态影响范围

#### `data/`

- 可与 `ui/` 并行
- 若修改卡牌定义结构、快照字段或运行时模型，必须先锁数据契约
- 若修改 DSL/IR 契约、导入链路或可执行卡牌定义格式，必须先冻结统一 IR 结构，再允许依赖方接入

#### `ui/`

- 可独立并行
- 前提是不擅自改变 `GameManager` 快照结构、信号语义和核心规则入口

#### `docs/` 与测试脚本

- 适合作为 sidecar 并行任务
- 可由独立 agent 补文档、补测试、补冒烟脚本

### 6.2 禁止并行的组合

- 两个 agent 同时改同一个 `.gd` 文件
- 一个 agent 改 `core/` 公共接口，另一个 agent 同时基于旧接口改 `ui/`
- 未锁定数据结构前，同时修改 `data/` 和依赖该结构的多个 `core/` 子模块
- 一边改规则语义，一边由别的 agent 根据旧语义补测试

### 6.3 默认拆分建议

#### 规则功能开发

- Agent A：改 `core/` 规则主逻辑
- Agent B：补 `docs/` 与测试脚本
- Agent C：做 UI 适配，只消费已锁定接口

#### 卡牌效果扩展

- Agent A：改 `EffectResolver` 和效果模型
- Agent B：改卡牌数据与样例
- Agent C：做冒烟用例，不改规则主逻辑

#### UI 打磨

- Agent A：改 `ui/`
- Agent B：做日志、快照字段或轻量接口补充
- Agent C：做交互验证，不改视觉实现

---

## 7. 卡牌 DSL/IR 设计原则

### 7.1 核心建模原则

- 卡牌效果系统的扩展原则是“按原子能力写代码”，不是“按卡写代码”。
- 不允许按单卡写专用规则代码。
- 新增能力必须优先抽象为可复用的原子能力、原子要求或原子步骤。
- DSL/IR 必须采用固定格式，优先通过少量稳定字段表达能力，不允许为个别卡临时发明新结构。
- DSL/IR 中必须严格区分“要求”和“步骤”。

### 7.2 要求与步骤边界

#### 要求

- 只负责可用性、合法性、触发条件与筛选判断
- 不得直接修改游戏状态

#### 步骤

- 只负责执行原子状态变化
- 不得承载额外条件分支、脚本式控制流或按卡特判

### 7.3 运行时约束

- 复杂卡效应通过“多个能力 + 原子步骤组合”表达，而不是在解释器里增加按卡分支。
- 特殊登场规则如 `RAID` 归入出牌规则层或专门的出牌规则模型，不混入普通效果步骤。
- 目标选择默认进入显式待决策流，不能用自动取前 N 个候选来替代玩家选择。
- 运行时只能消费统一可执行 IR，不直接执行原始效果文本、OCR 文本或语义说明文本。
- 无法被现有 IR 表达的卡牌，应先扩展原子要求或原子步骤，再考虑接入；禁止直接为该卡补专用解释逻辑。

### 7.4 设计与验收要求

- 涉及 `EffectResolver`、卡牌定义结构、导入链路或运行时效果模型时，主 agent 必须先冻结 IR 契约，再并行安排核心、数据、测试或 UI 适配。
- 原始卡牌文本、语义整理结果与可执行 IR 必须分层管理；只有统一 IR 可以作为正式对局数据源。
- 如果某张卡当前无法收敛到既定 DSL/IR，应明确标记为“暂不支持”，而不是回退到按卡硬编码。
- 主 agent 在集成前必须额外检查：是否引入了按卡硬编码、要求/步骤混用、偏离固定格式的临时字段，或让原始文本直接参与运行时结算。

---

## 8. 测试与验收要求

### 8.1 基本要求

所有规则相关改动至少补一种验证：

- 单元测试
- 状态机测试
- 结算流程测试
- 最小对局样例测试
- 冒烟测试脚本

### 8.2 优先覆盖范围

优先覆盖：

- 攻击 / 阻挡结算
- 生命区触发
- 区域容量上限
- AP 与能量支付
- 回合切换与状态恢复

### 8.3 UI / 布局验收要求

涉及以下界面或场景的改动后，必须额外检验“手牌区域不允许遮挡战场区域”：

- `battle_scene`
- `hand_view`
- `board_view`
- `card_view`
- `scenes/battle_scene.tscn`

若无法直接目视验证，至少补充对应的布局检查说明或验证记录。未完成该检验的改动视为未完成。

### 8.4 主 agent 集成前检查项

- 是否越界修改了未授权目录或文件
- 是否破坏 `rule.md` 约束
- 是否引入跨模块隐式耦合
- 是否引入按卡硬编码、要求/步骤混用、偏离固定格式的临时 DSL/IR 字段
- 是否修改了接口却没有同步说明依赖方影响
- 是否补了必要验证
- 是否与并行分支的接口假设一致

### 8.5 合格交付判定

满足以下条件，方可视为本轮改动完成：

- 规则未偏离 `rule.md`
- 写入范围未越界
- 无同文件并行冲突
- 已完成至少一种有效验证
- 已同步计划、README、日志口径
- 中文文档无乱码或编码污染
- 若涉及 UI，已确认手牌区域无遮挡

---

## 9. Godot 与中文文档注意事项

### 9.1 工程运行

- 除非任务明确要求，否则不要手动编辑 `.uid` 文件。
- 编辑 `project.godot` 时保持改动最小且目的明确。
- 保持 `res://` 路径有效。
- 修改脚本后优先做一次 headless 启动或冒烟测试。
- 运行 Godot 冒烟测试使用项目根目录路径下的 `\Godot\Godot_v4.6.1-stable_win64_console.exe` 执行。
- 默认只在沙箱外运行 Godot；若在沙箱内运行出现 `user://logs` 写入失败、启动失败或 Godot 崩溃，不要继续重试沙箱内命令，应直接申请沙箱外执行。

### 9.2 中文文档读取

涉及以下内容时，必须优先使用显式 UTF-8 方式读取，而不是依赖默认终端输出：

- `docs/rules/*.md`
- `docs/plan/*.md`
- `docs/logs/*.md`
- `docs/refactor/*.md`
- 含中文 UI 文案、提示文本或断言信息的 `.gd` 文件
- 任何会被脚本读取或写回的中文 Markdown 文档

推荐方式：

- 单文件全文：`[System.IO.File]::ReadAllText(path, [System.Text.UTF8Encoding]::new($false))`
- 按行取片段：`[System.IO.File]::ReadAllLines(path, [System.Text.UTF8Encoding]::new($false))`

若默认输出与显式 UTF-8 输出不一致，以显式 UTF-8 结果为准。

### 9.3 中文文档编辑优先级

修改中文文档时，优先级固定为：

1. `apply_patch`
2. 多行 here-doc / here-string 配合显式 UTF-8 脚本处理
3. Python 显式 `encoding='utf-8'` 直接读写

不再把 PowerShell 控制台当作文档编辑器。

禁止以下做法：

- 直接依赖默认 `Get-Content` 输出做规则、计划、日志结论
- 先在 PowerShell 控制台中读取中文，再拼接字符串回写文件
- 用终端展示结果反推文件真实编码状态
- 依赖 `Out-File`、`Set-Content` 或未显式编码的 `WriteAllText` 回写中文文档
- 把中文正文塞进单行 `python -c "..."` 或 `powershell -Command "..."` 再直接写回文件
- 禁止把中文正文直接内联到需要提权的终端命令、here-string、here-doc 或跨 shell 命令链中再写回文件；即使最终调用方声明 `encoding='utf-8'`，只要中文内容先在命令传参链里被替换成 `?`，落盘后仍会形成不可逆污染
- 不要因为终端回显出现 `??` 就假定只是“显示问题”；若脚本下一步会把该文本写回文件，则这些 `?` 会作为真实内容落盘
- 涉及中文 `.gd`、`.tscn`、`.md` 时，不要用“整文件 replace + 整文件回写”替代小补丁；一旦源文本已被污染，会把正常中文成片覆盖成问号串

### 9.4 中文文档修改后校验

每次修改中文文档后，必须立即做两类非展示型校验：

- 用显式 UTF-8 重新读取目标文件，确认未出现异常 `?`、`????`、替代字符或明显乱码片段
- 执行 `git diff` 静态复核，确认未把中文段落替换成问号串、旧英文口径或错位段落

若仓库内存在 `tools/check_utf8_docs.py`，中文文档变更后应优先补跑一次；该脚本只做告警与定位，不自动改写文件。

---

## 10. 异常处理原则

### 10.1 `apply_patch` 失败

- 优先切换到显式 UTF-8 的 Python / 脚本文件方案
- 不优先退回 PowerShell 文本替换
- 中文文档批量修改时，应优先拆成小补丁
- 若 `apply_patch` 在 Windows 上因 sandbox refresh 失败，不要立刻改用携带中文正文的整文件重写；优先改成按锚点、行号、ASCII 标记或 `unicode_escape` 的最小脚本修改
- 若同一文件连续两次 `apply_patch` 失败，应先重新读取目标文件并做 `git diff`，确认上下文未漂移，再决定是否改用脚本方案

### 10.2 Godot 沙箱运行失败

- 若 Godot 在沙箱内出现 `user://logs` 写入失败、启动失败或崩溃，不要继续重复尝试
- 直接改为沙箱外执行

### 10.3 中文乱码落盘

- 先停止继续编辑
- 用显式 UTF-8 复读确认污染范围
- 再做最小修复
- 不允许在乱码状态下继续叠加修改
- 若可从 `git show HEAD:<path>`、最近提交或已知干净版本重建，应优先回到干净基线后再重放本轮最小改动，不要在问号串之上继续局部修补

### 10.4 worker 越界修改

- 立即回滚越界部分或拆分为主 agent 重新裁定
- 不得默认接受“顺手一起改了”的扩散修改
- 主 agent 集成前必须明确标记越界影响

### 10.5 接口冻结后发现不兼容

- 先由主 agent 重新定案
- 再统一更新依赖方任务边界
- 不允许依赖方各自按理解临时修补

---

## 11. 交付与模板

### 11.1 主 agent 任务单模板

```md
## 任务单
- 目标：
- 写入范围：
- 禁止修改：
- 依赖接口：
- 风险点：
- 完成判定：
- 验证要求：
````

### 11.2 worker 交付模板

```md
## 交付摘要
- 修改目标：
- 实际修改范围：
- 影响接口/信号/数据结构：
- 未解决问题或风险：
- 建议复查点：
- 验证方式与结果：
```

### 11.3 日志模板

```md
## yyyy-MM-dd
- 变更类型：
- 变更摘要：
- 影响文件：
- 验证方式：
- 结果：
- 后续事项：
```

---

## 12. 推荐工作流

### 12.1 主 agent 工作流

1. 确认需求是否触碰高风险规则区
2. 判断本轮应保持串行还是值得引入子 agent 并行
3. 锁定接口、数据契约和写入边界
4. 若涉及卡牌效果、导入链路或 `EffectResolver`，先冻结 DSL/IR 契约与“要求/步骤”边界
5. 把实现、测试、UI 适配拆成互不冲突的任务
6. 收齐结果后统一集成与验收
7. 同步更新计划、README 与日志

### 12.2 单 agent 模式补充说明

即使不启用多 agent，也仍应遵守以下原则：

* 先锁边界，再开始实现
* 高风险规则区仍按串行策略处理
* 接口、数据、DSL/IR 契约仍需先明确再改动
* 测试、日志、计划、README 的同步要求不变
* 中文文档与 UI 验收要求不变

---

## 13. 验收标准

本规范的目标是让实现者在开始一轮开发前，不需要再自行决定以下问题：

* 怎么拆任务
* 哪些部分可以并行
* 哪些边界不能越过
* 子 agent 如何交接
* 主 agent 如何验收
* 失败后如何处理
* 中文文档如何安全编辑
* 文档与代码如何同轮保持一致

如果遇到本文档未覆盖、但会影响规则语义的情况，默认回到 `rule.md`，并由主 agent 定案。

---
