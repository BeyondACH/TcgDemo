# AGENTS.md

本文档是本仓库的 Codex 多 agent 协作规范。目标不是重复项目说明，而是让主 agent 与 worker / reviewer 在同一轮开发中可以稳定拆分、并行推进、清晰交接，并始终保持规则实现与 `rule.md` 一致。

## 1. 项目事实与权威来源

### 项目概况

- 引擎：Godot 4.6
- 语言：GDScript
- 入口场景：`res://scenes/battle_scene.tscn`
- 项目类型：本地 1v1 卡牌对战原型，规则设计参考 UNION ARENA 风格

### 权威规则来源

- `rule.md` 是本项目规则、状态机、区域限制、结算顺序和数据映射的最高权威来源。
- 任何代码实现、测试、重构、示例数据、注释说明，只要与 `rule.md` 冲突，都必须以 `rule.md` 为准。
- 如果用户需求与 `rule.md` 冲突，不要直接实现冲突方案；先指出冲突点，再给出遵循 `rule.md` 的实现建议。

### 开发计划文件

- 项目当前的正式开发计划保存在 `docs/plan/project_development_plan.md`。
- 主 agent 在进入需求拆分、接口冻结、并行分工、阶段验收前，应先对齐该计划与 `docs/rules/rule.md`。
- 如果新的需求会改变里程碑优先级、接口冻结项或测试策略，应先更新 `docs/plan/project_development_plan.md`，再组织多 agent 实施。

### 当前目录职责

- `core/`：回合流程、规则校验、战斗、效果结算、胜负判断、区域管理
- `data/`：运行时状态模型、卡牌定义、卡组数据
- `ui/`：界面层、交互脚本、快照消费
- `scenes/`：Godot 场景
- `docs/`：测试脚本、说明文档、辅助资料

### 不可破坏的规则约束

- 前线最多 4 张角色
- 能量线最多 4 张卡
- AP 区域最多 3 槽
- 主卡组 50 张，起手 7 张，生命区初始 7 张
- 只有 ACTIVE 的前线角色可以攻击或阻挡
- 场地牌只能进入能量线，不能进入前线
- 胜负判断至少覆盖生命归零与空牌库抽牌失败
- 生命区触发、攻击/阻挡、区域容量、AP 与能量支付都属于高风险规则区

## 2. 卡牌 DSL/IR 设计原则

### 核心建模原则

- 卡牌效果系统的扩展原则是“按原子能力写代码”，不是“按卡写代码”。
- 不允许按单卡写专用规则代码；新增能力必须优先抽象为可复用的原子能力、原子要求或原子步骤。
- 卡牌 DSL/IR 必须采用固定格式，优先通过少量稳定字段表达能力，不允许为个别卡临时发明新结构。
- DSL/IR 中必须严格区分“要求”和“步骤”。
- 要求只负责可用性、合法性、触发条件与筛选判断，不得直接修改游戏状态。
- 步骤只负责执行原子状态变化，不得承载额外条件分支、脚本式控制流或按卡特判。
- 复杂卡效应通过“多个能力 + 原子步骤组合”表达，而不是在解释器里增加按卡分支。
- 特殊登场规则如 `RAID` 归入出牌规则层或专门的出牌规则模型，不混入普通效果步骤。
- 目标选择默认进入显式待决策流，不能用自动取前 N 个候选来替代玩家选择。
- 运行时只能消费统一可执行 IR，不直接执行原始效果文本、OCR 文本或语义说明文本。
- 无法被现有 IR 表达的卡牌，应先扩展原子要求或原子步骤，再考虑接入；禁止直接为该卡补专用解释逻辑。
- 任何对 DSL/IR 的扩展，都必须优先证明其可复用于多张卡和多类效果；若不能证明，默认不应进入正式模型。

### 设计与验收约束

- 涉及 `EffectResolver`、卡牌定义结构、导入链路或运行时效果模型时，主 agent 必须先冻结 IR 契约，再并行安排核心、数据、测试或 UI 适配。
- 原始卡牌文本、语义整理结果与可执行 IR 必须分层管理；只有统一 IR 可以作为正式对局数据源。
- 如果某张卡当前无法收敛到既定 DSL/IR，应明确标记为“暂不支持”，而不是回退到按卡硬编码。
- 主 agent 在集成前必须额外检查：是否引入了按卡硬编码、要求/步骤混用、偏离固定格式的临时字段，或让原始文本直接参与运行时结算。

## 3. 多 Agent 协作原则

### 角色定义

- 主 agent：
  - 负责理解需求、制定拆分方案、分配写入边界、整合结果、最终验收
  - 负责所有跨模块决策，尤其是规则语义、接口契约、状态流转、冲突裁决
  - 负责决定哪些任务可以并行，哪些必须串行
- worker agent：
  - 只实现被分配的明确范围
  - 不自行扩大修改面，不擅自重构公共接口
  - 遇到跨模块依赖时，只报告阻塞、假设和建议，不自行拍板
- reviewer / checker agent：
  - 只做验证、回归检查、风险枚举和验收建议
  - 默认不与实现 agent 修改同一写入面

### 协作基本规则

- 先锁边界，再并行开发
- 同一轮并行任务中，每个 agent 必须有明确写入范围
- 不允许两个 agent 同时修改同一个 `.gd` 文件
- 不允许为了并行方便而擅自改变 `rule.md` 语义
- 不允许把 UI 层逻辑反向塞入规则核心
- 主 agent 必须在接口冻结后再让依赖方并行接入

### 决策边界

- 规则语义、状态机、触发顺序、区域容量、资源支付：只能由主 agent 定案
- 局部实现细节、内部函数拆分、低风险文案调整：worker 可在边界内自主处理
- 一旦涉及 `core + data + ui` 跨层联动，默认先由主 agent 锁接口，再分派实现

## 4. 并行分工边界

### 可并行工作面

- `core/`
  - 高风险区
  - 同一轮并行中，默认只允许 1 个 agent 修改同一个规则子系统
  - 可按 `turn / battle / effect / zone / victory` 子模块拆分
  - 前提是主 agent 先锁定接口和状态影响范围
- `data/`
  - 可与 `ui/` 并行
  - 若修改卡牌定义结构、快照字段或运行时模型，必须先锁数据契约
  - 若修改 DSL/IR 契约、导入链路或可执行卡牌定义格式，必须先冻结统一 IR 结构，再允许依赖方并行接入
- `ui/`
  - 可独立并行
  - 前提是不擅自改变 `GameManager` 快照结构、信号语义和核心规则入口
- `docs/` 与测试脚本
  - 最适合作为 sidecar 并行任务
  - 可由独立 agent 补文档、补测试、补冒烟脚本

### 禁止并行的组合

- 两个 agent 同时改同一个 `.gd` 文件
- 一个 agent 改 `core/` 公共接口，另一个 agent 同时基于旧接口改 `ui/`
- 未锁定数据结构前，同时修改 `data/` 和依赖该结构的多个 `core/` 子模块
- 一边改规则语义，一边由别的 agent 根据旧语义补测试

### 默认拆分建议

- 规则功能开发：
  - Agent A 改 `core/` 规则主逻辑
  - Agent B 补 `docs/` 与测试脚本
  - Agent C 做 UI 适配，只消费已锁定接口
- 卡牌效果扩展：
  - Agent A 改 `EffectResolver` 和效果模型
  - Agent B 改卡牌数据与样例
  - Agent C 做冒烟用例，不改规则主逻辑
- UI 打磨：
  - Agent A 改 `ui/`
  - Agent B 做日志、快照字段或轻量接口补充
  - Agent C 做交互验证，不改视觉实现

## 5. 交接与验收协议

### 每个 worker 的交付格式

每个子 agent 交付时必须至少包含：

- 修改目标
- 实际修改范围
- 影响到的接口、信号或数据结构
- 未解决问题或剩余风险
- 建议主 agent 复查的点
- 验证方式与结果

### 主 Agent 集成前检查项

- 是否越界修改了未授权目录或文件
- 是否破坏 `rule.md` 约束
- 是否引入跨模块隐式耦合
- 是否引入按卡硬编码、要求/步骤混用、偏离固定格式的临时 DSL/IR 字段
- 是否修改了接口却没有同步说明依赖方影响
- 是否补了必要验证
- 是否与并行分支的接口假设一致

### 推荐交接模板

建议统一按以下格式汇报：

```md
## 交付摘要
- 修改目标：
- 实际修改范围：
- 影响接口/信号/数据结构：
- 未解决问题或风险：
- 建议复查点：
- 验证方式与结果：
```

## 6. 本项目推荐协作模式

### 需求进入时的主 Agent 工作流

1. 先确认需求是否触碰 `rule.md` 高风险规则区
2. 判断是串行开发还是可拆并行
3. 锁定接口、数据契约和写入边界
4. 若需求涉及卡牌效果、导入链路或 `EffectResolver`，先冻结 DSL/IR 契约与“要求/步骤”边界
5. 把实现、测试、UI 适配拆成互不冲突的任务
6. 收齐结果后统一集成与验收

### 推荐串行场景

- 修改回合状态机
- 修改攻击/阻挡结算顺序
- 修改触发系统和效果执行模型
- 修改卡牌 DSL/IR 契约、原子步骤集合或导入到运行时 IR 的编译链路
- 修改 `GameManager` 快照结构且多个模块都依赖它
- 大范围数据结构迁移

### 推荐并行场景

- 已锁接口前提下的 UI 适配
- 文档补充与规则说明整理
- 冒烟测试、回归测试、样例数据补齐
- 相互独立的 `core/` 子系统小改动
- 在 DSL/IR 契约已冻结前提下的数据补录、样例卡编写与 UI 消费适配

### 测试与验证要求

所有规则相关改动至少补一种验证：

- 单元测试
- 状态机测试
- 结算流程测试
- 最小对局样例测试
- 冒烟测试脚本
- 涉及 `battle_scene`、`hand_view`、`board_view`、`card_view` 或 `scenes/battle_scene.tscn` 的界面/布局改动后，必须检验“手牌区域不允许遮挡战场区域”；未完成该检验的改动视为未完成

优先覆盖：

- 攻击/阻挡结算
- 生命区触发
- 区域容量上限
- AP 与能量支付
- 回合切换与状态恢复

### Godot 特别注意事项

- 除非任务明确要求，否则不要手动编辑 `.uid` 文件
- 编辑 `project.godot` 时保持改动最小且目的明确
- 保持 `res://` 路径有效
- 修改脚本后优先做一次 headless 启动或冒烟测试
- 若在 Codex 沙箱内运行 Godot 冒烟测试受限或结果不稳定，可在获得批准后使用 `D:\CodexWork\TcgDemo\Godot\Godot_v4.6.1-stable_win64_console.exe` 走沙箱外执行；主 agent 需在交付中明确记录所用命令、是否为沙箱外运行，以及验证结果
- 每次提交 UI/布局相关改动后，都必须额外确认手牌区域没有遮挡战场区域；若无法直接目视验证，至少补充对应的布局检查说明或验证记录
- 仓库中的代码文件统一使用 UTF-8 编码，新增文件与修改现有代码文件时都应保持为 UTF-8，以避免中文乱码问题
- 若终端出现中文乱码，先确认文件编码和换行，再决定是否重写

## 验收标准

这份文档的目标是让实现者在开始一轮多 agent 开发前，不需要再自行决定：

- 怎么拆任务
- 哪些部分可以并行
- 哪些边界不能越过
- 子 agent 应该如何交接
- 主 agent 应该如何做最终验收

如果遇到文档未覆盖但会影响规则语义的情况，默认回到 `rule.md`，并由主 agent 决定方案。

## 7. Change Log Requirement

- 每次功能更新与每次 bugfix，都必须在同一轮改动中同步记录到 `docs/logs.md`。
- 主 agent 在开始实现前，应先检查并读取 `docs/logs.md` 中最近的相关记录。
- 所有写入 `docs/logs.md` 的内容必须使用中文描述，不允许使用英文摘要、英文模板或英文结论。
- 实现完成后，交付 agent 必须向 `docs/logs.md` 追加一条简明记录，至少包含：
  - 日期
  - 变更类型（功能更新或 bugfix）
  - 变更摘要
  - 影响文件或模块
  - 验证方式与结果
- 如果某次改动没有写入 `docs/logs.md`，则该次工作应视为未完成。
- 多 agent 协作时，由主 agent 负责确保最终集成结果只在 `docs/logs.md` 中记录一次，且内容为中文。
