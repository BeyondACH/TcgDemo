# 2026-03-27 变更日志

## 2026-03-27

- 变更类型：功能更新
- 变更摘要：调整多 agent 协作规范中的日志存储方式，后续改为按天在 `docs/logs/` 目录下创建并维护 `log_yyyy-MM-dd.md` 文件，不再要求统一追加到单一 `docs/logs.md`。
- 影响文件或模块：`AGENTS.md`、`docs/logs/log_2026-03-27.md`
- 验证方式与结果：代码检查确认 `AGENTS.md` 第 7 节已改为按日日志规则，并已创建当日日志文件作为样例，命名格式与要求一致。

- 变更类型：功能更新
- 变更摘要：落地 cards_effects 的 P0 第一批通用模板，新增“从手牌过滤登场角色”“optional 前置动作成功后继续结算”“从 OUTSIDE 过滤检索到手”三类 IR 能力，并补齐 `UA31BT_MMM_1_082`、`UA31ST_MMM_1_102`、`UA31ST_MMM_1_108` 与 `UA31BT_MMM_1_075` 前半段的编译和运行时支持。
- 影响文件或模块：`core/effect_resolver.gd`、`core/game_manager.gd`、`tools/compile_cards_effects.py`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/project_development_plan.md`
- 验证方式与结果：执行 `python tools/compile_cards_effects.py` 成功，支持能力由 59 提升到 63、未支持能力由 14 降到 11；在沙箱外执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 15 项通过、0 项失败，输出 `CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`；在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 23 项通过、0 项失败。两次 Godot 退出时仍有既有资源泄漏告警，但未影响断言通过。

- 变更类型：功能更新
- 变更摘要：补齐 `UA31BT_MMM_1_075` 的 P0 剩余能力，新增“临时授予不能攻击直到下个自己回合开始”的运行时生命周期与攻击合法性校验，并将该效果完整接入 cards_effects 编译链与最小对局验证。
- 影响文件或模块：`core/effect_resolver.gd`、`core/rules_engine.gd`、`core/turn_manager.gd`、`core/game_manager.gd`、`tools/compile_cards_effects.py`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/tolist.md`、`docs/plan/project_development_plan.md`
- 验证方式与结果：执行 `python tools/compile_cards_effects.py` 成功，未支持能力由 11 降到 10；在沙箱外执行 `--headless --quit` 启动项目成功；在沙箱外执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 16 项通过、0 项失败，新增覆盖“RAID 登场后限制对手角色不能攻击，并在下个自己回合开始时失效”；在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 23 项通过、0 项失败。Godot 退出时仍有既有资源泄漏告警，但未影响断言通过。

- 变更类型：功能更新
- 变更摘要：在卡牌预览面板中为临时关键词补充可读展示，将 `CANNOT_ATTACK` 显示为“不能攻击（直到下个自己回合开始）”，便于确认 `UA31BT_MMM_1_075` 附加的限制状态。
- 影响文件或模块：`ui/card_preview_panel.gd`、`docs/logs/log_2026-03-27.md`
- 验证方式与结果：代码检查确认预览面板的临时关键词展示已走文案映射；在沙箱外执行 `--headless --quit` 启动项目成功，脚本可正常加载。启动过程中仍有既有布局 anchor 告警，但未影响本次展示改动验证。

- 变更类型：功能更新
- 变更摘要：优化 `card_preview_panel` 预览区展示，新增空态与预览来源文案，并支持点击对手前线与能量线卡牌时进入预览而不误触发己方动作选中。
- 影响文件或模块：`ui/battle_scene.gd`、`ui/card_preview_panel.gd`
- 验证方式与结果：完成 UI 代码检查，确认预览态与可操作选中态已拆分；执行 Godot `--layout-probe` 成功，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认预览区常驻后手牌区域未遮挡战场。运行中仍出现既有的 Control anchor warning，但未影响本次布局验收通过。

- 变更类型：功能更新
- 变更摘要：落地 `todo.md` 核心 P1 能力，新增动态数值 `value_provider`、`CARD_BP_LTE_DYNAMIC`、条件化阈值替换、公开结果驱动奖励与固定 fallback 主链/后备链表达，补齐 `UA31BT_MMM_1_078`、`UA31BT_MMM_1_085`、`UA31ST_MMM_1_085`、`UA31BT_MMM_1_093`、`UA31BT_MMM_1_094`、`UA31ST_MMM_1_094`、`UA31BT_MMM_1_098` 的编译与运行时支持。
- 影响文件或模块：`core/effect_resolver.gd`、`tools/compile_cards_effects.py`、`data/cards/cards_effects.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/project_development_plan.md`、`docs/plan/todo.md`
- 验证方式与结果：执行 `python tools/compile_cards_effects.py` 成功，支持能力由 63 提升到 70、未支持能力由 10 降到 3；在沙箱外执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 21 项通过、0 项失败，输出 `CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`，新增覆盖动态 BP 上限、条件化阈值替换、公开结果奖励与 fallback 主链/后备链；在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 23 项通过、0 项失败。两次 Godot 退出时仍有既有资源泄漏告警，但未影响断言通过。

- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：为 `removed` 与 `outside` 区域新增点击查看功能，点击战场侧边堆叠摘要后会弹出临时窗口，展示对应区域全部卡牌的缩略图。
  - 影响文件或模块：`core/game_manager.gd`、`ui/zone_stack_summary_view.gd`、`ui/board_view.gd`、`ui/battle_scene.gd`、`ui/zone_cards_popup.gd`
  - 验证方式与结果：在沙箱外执行 Godot `--headless --quit` 启动成功；执行 `--resolution 1920x1080 -- --layout-probe` 输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认新增弹窗入口后手牌区域仍未遮挡战场区域。运行中仍存在既有的 Control anchor warning，但未影响本次功能与布局验证通过。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：完成剩余 P1 能力，补齐 `UA31BT_MMM_1_070`、`UA31ST_MMM_1_070` 的临时产能与主阶段结束自退场模板，并落地 `UA31BT_MMM_1_090` 绑定自身的临时特殊登场/RAID 许可；同时将 `life_trigger_only` 的手牌 RAID 语义收敛为“默认不允许，需生命触发或临时许可放行”。
  - 影响文件或模块：`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`core/rules_engine.gd`、`core/game_manager.gd`、`core/turn_manager.gd`、`data/cards/cards_effects.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/hand_available_actions_smoke_test.gd`、`docs/plan/todo.md`、`docs/plan/project_development_plan.md`
  - 验证方式与结果：执行 `python tools/compile_cards_effects.py` 成功，支持能力提升到 73、未支持能力降到 0；在沙箱外执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 23 项通过、0 项失败，输出 `CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`；在沙箱外执行 `res://docs/hand_available_actions_smoke_test.gd`，结果为 15 项通过、0 项失败；在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 23 项通过、0 项失败。Godot 退出时仍有既有 `ObjectDB`/resource 泄漏告警，但未影响断言通过。

- 变更类型：功能更新
- 变更摘要：在 `docs/plan/mile_stone.md` 追加“后续开发计划（2026-03-27）”章节，保持既有 M1-M6 盘点内容不变，并补充 P0 效果系统剩余缺口收口、P1 规则层与出牌规则层补强、P2 验证资产扩充，以及推荐实施顺序。
- 影响文件或模块：`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
- 验证方式与结果：代码检查确认新增内容已追加在第 6 节之后，结构包含 `## 7. 后续开发计划（2026-03-27）`、`P0`、`P1`、`P2` 与“推荐实施顺序”，且未改动前文 M1-M6 盘点结论。

- 变更类型：bugfix
- 变更摘要：修正 `docs/plan/todo.md` 中 `UA31BT_MMM_1_078` 的能力缺口状态，将过期的“缺：动态 BP 上限 requirement”更新为“已补：动态 BP 上限 requirement”，使卡牌缺口映射与上文已完成状态、编译链与运行时实现保持一致。
- 影响文件或模块：`docs/plan/todo.md`、`docs/logs/log_2026-03-27.md`
- 验证方式与结果：代码检查确认 `todo.md` 前文已存在 `[x] 动态 BP 上限 requirement`、`覆盖 078` 等完成记录；本次仅同步修正文档尾部卡牌缺口映射中的过期表述，未改动运行时代码。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：清理 `ui/battle_scene.gd` 中与全局 `class_name` 重名的 `preload` 常量，并消除局部变量遮蔽与未使用参数告警，减少 `battle_scene` 脚本静态检查噪音。
  - 影响文件或模块：`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--headless --quit` 启动成功；执行 `--resolution 1920x1080 -- --layout-probe` 输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认手牌区域未遮挡战场区域。运行中仍存在既有的 Control anchor warning，但不属于本次修复范围。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：将 `docs/plan/mile_stone.md` 中后续开发计划的 P1 段落改写为按当前仓库状态对齐的拆分清单，明确已完成基线、待执行任务、写入边界与推荐顺序，避免重复推进已落地的规则层工作。
  - 影响文件或模块：`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：代码检查确认 P1 段落已从目标描述调整为 `已完成基线 + 待拆分执行清单 + 建议写入边界 + 当前推荐顺序` 结构，并与 `docs/plan/project_development_plan.md`、`docs/plan/todo.md` 当前状态保持一致。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：收紧 `docs/plan/mile_stone.md` 中 P1 拆分清单的执行优先级，明确先稳定 DRAW 阶段验证入口，再补回合/AP 与生命触发两个高风险规则区的专项回归。
  - 影响文件或模块：`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：代码检查确认 P1 段落中的顺序描述已调整为 `当前执行优先级`，且首两档优先级明确固定为 `P1-5` 与 `P1-1/P1-3`，与当前推进意图一致。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：完成 P1-5 的首轮稳定性核验，确认 `docs/draw_phase_smoke_test.gd` 在当前环境下可继续作为 DRAW 阶段回归入口使用，并同步更新 `mile_stone` 中的风险描述与优先级状态。
  - 影响文件或模块：`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外单次执行 `res://docs/draw_phase_smoke_test.gd` 输出 `DRAW_PHASE_SMOKE_OK`；随后连续 5 次重跑全部通过，输出 `DRAW_PHASE_REPEAT_OK`。Godot 退出时仍存在既有 `ObjectDB` / resource 泄漏告警，但未影响脚本断言通过。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：完成 P1-1 与 P1-3 的首轮回归补强，在 `docs/milestone_smoke_test.gd` 中新增“END 阶段 AP 不会恢复”与“生命触发可选跳过但伤害流程继续”两项专项断言，并同步推进 `mile_stone` 中的任务状态。
  - 影响文件或模块：`docs/milestone_smoke_test.gd`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 25 项通过、0 项失败；复跑 `res://docs/draw_phase_smoke_test.gd` 输出 `DRAW_PHASE_SMOKE_OK`。Godot 退出时仍存在既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：补充终端中文读取协作约定，明确关键中文片段必须先用显式 UTF-8 方式重读，避免基于 PowerShell 默认编码或终端代码页噪音对规则、计划与日志内容做误判。
  - 影响文件或模块：`AGENTS.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：代码检查确认 `AGENTS.md` 已新增显式 UTF-8 读取约定、推荐命令与关键文件范围；并分别用默认读取与 `[System.IO.File]::ReadAllLines(path, [System.Text.UTF8Encoding]::new($false))` 抽查 `docs/plan/mile_stone.md` 和 `docs/logs/log_2026-03-27.md`，可作为后续中文内容判断基准。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：完成 P1 规则层与出牌规则层补强的剩余收口，补齐“攻击失败的攻击方不会退场”与“同时触发顺序”专项回归；其中 battle 同时触发补入“回合方先处理”的最小规则实现，并把同方多触发顺序验证接入 `milestone` 主冒烟脚本。
  - 影响文件或模块：`core/effect_resolver.gd`、`core/game_manager.gd`、`core/battle_resolver.gd`、`docs/milestone_smoke_test.gd`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 27 项通过、0 项失败；复跑 `res://docs/draw_phase_smoke_test.gd` 输出 `DRAW_PHASE_SMOKE_OK`。Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：落地 P2 首轮“验证资产扩充与稳定性建设”，将 `docs/milestone_smoke_test.gd` 固定为规则主冒烟入口，并在 `docs/cards_raw_minimal_duel_smoke_test.gd` 新增 8 条正式 raw 样例，覆盖临时产能修饰、延迟自退场、绑定自身的临时特殊登场 / `RAID` 许可，以及看牌堆顶后的条件追加结算边界。
  - 影响文件或模块：`docs/milestone_smoke_test.gd`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 27 项通过、0 项失败；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 31 项通过、0 项失败，输出 `CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`。两次 Godot 退出时仍存在既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：根据当前项目现状收敛 `docs/plan/mile_stone.md`，同步更新时间、M4 能力统计、P0/P1/P2 阶段状态与后续重点，使里程碑盘点与今日已完成的 DSL/IR 收口、规则回归和 raw 样例回归结果保持一致。
  - 影响文件或模块：`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：代码检查确认 `mile_stone.md` 已更新为 `2026-03-27`，并将统一 DSL/IR 状态修正为 73 个已支持能力、0 个未支持能力；同时将 P0 标记为已完成、P1 保持已收口、P2 标记为当前主线，文档内容与 `docs/plan/project_development_plan.md` 及今日日志记录一致。

- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：统一更新 `docs/plan/project_development_plan.md`、`docs/plan/mile_stone.md` 与 `README.md` 的项目现状口径，将开发重点收敛为“规则主冒烟维护 + 正式 raw 样例扩充 + 稳定性建设”，并同步修正 README 中过期的能力统计、验证结果与日志路径说明。
  - 影响文件或模块：`docs/plan/project_development_plan.md`、`docs/plan/mile_stone.md`、`README.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：代码检查确认三份文档已统一到当前基线：规则主冒烟为 27 项通过、raw 样例冒烟为 31 项通过、统一 DSL/IR 为 73 个已支持能力且 0 个未支持能力；README 中日志要求已改为按日日志 `docs/logs/log_yyyy-MM-dd.md`，并补充 DRAW 阶段专项与布局验收入口说明。

- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：扩充 `docs/cards_raw_minimal_duel_smoke_test.gd` 的第一批高风险正式 raw 样例，新增 8 条正式卡回归，覆盖 `068` 手牌条件减费、`101` 登场抽弃、`091` 场地 ACTIVE 登场与主动技目标增益、`096` AP 显式选择与 Final 补命、`083` 生命区回手后双击 RAID、`104` 按名称种类数分段获得效果的 RAID；同时补齐编译链与运行时对手牌能量减费、场地 enter_state、AP 显式选择、turn flag / battle outcome requirement，以及生命窗口结束后 effect queue 自动恢复的通用支持。
  - 影响文件或模块：`docs/cards_raw_minimal_duel_smoke_test.gd`、`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`core/game_manager.gd`、`core/battle_resolver.gd`、`core/rules_engine.gd`、`core/turn_manager.gd`、`core/zone_manager.gd`、`data/game_state.gd`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/plan/card_design.md`
  - 验证方式与结果：执行 `python tools/compile_cards_effects.py` 成功，支持能力提升到 76、未支持能力保持 0；在沙箱外执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 39 项通过、0 项失败，输出 `CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`；在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 27 项通过、0 项失败；在沙箱外执行 `res://docs/hand_available_actions_smoke_test.gd`，结果为 15 项通过、0 项失败。Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。

- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：按 `rule.md` 口径收回 AP 自动恢复效果的显式选择实现，将 `ACTIVATE_AP_SLOTS` 恢复为“自动将最多 N 张休息 AP 恢复为活跃”，同步修正正式 raw 样例与能力设计文档中的过期描述。
  - 影响文件或模块：`core/effect_resolver.gd`、`tools/compile_cards_effects.py`、`data/cards/cards_effects.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/card_design.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：执行 `python tools/compile_cards_effects.py` 成功，支持能力保持 76、未支持能力保持 0；在沙箱外执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 39 项通过、0 项失败；在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 27 项通过、0 项失败。Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。

- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：将 `SimpleAI` 从静态优先级选择器升级为轻量评分式 `v2`，补齐 `MOVE / MAIN / ATTACK / BLOCK / STEP_SWAP_CHOICE / HAND_LIMIT_DISCARD` 的阶段评分选择逻辑，并新增独立 AI 专项冒烟脚本验证直攻优先、斩杀优先、`NO_BLOCK` 比较与低价值弃换牌选择。
  - 影响文件或模块：`core/ai/simple_ai.gd`、`docs/simple_ai_v2_smoke_test.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/simple_ai_v2_smoke_test.gd`，结果为 6 项通过、0 项失败；执行 `res://docs/vs_ai_smoke_test.gd`，结果为 2 项通过、0 项失败。两次 Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响本次 AI 回归通过。

- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：更新 `mile_stone` 盘点文档，补入 `SimpleAI v2` 的评分式 AI 基线、AI 专项 smoke 与整体流程 smoke 的验证分层，并将正式计划路径口径统一为当前仓库实际存在的 `docs/plan/development_plan.md`。
  - 影响文件或模块：`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：代码检查确认里程碑文档已新增 `M5.5` AI 对局启发式基线、更新验证脚本列表与验证基线，并同步补充当前 AI 风险与下一阶段建议；文档内容与 `SimpleAI v2` 实现、`simple_ai_v2_smoke_test.gd` 和 `vs_ai_smoke_test.gd` 的最新结果一致。

- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：补充 `AGENTS.md` 的开发计划约束，明确主 agent 每次制定开发计划时都要结合任务复杂度、跨模块依赖与上下文消耗量，先判断是否值得引入子 agent 并行实施，避免为并行而并行。
  - 影响文件或模块：`AGENTS.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：代码检查确认新增规则已写入“开发计划文件”与“需求进入时的主 Agent 工作流”两处，表述与现有多 agent 协作边界保持一致，未改动其他规则语义。

- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：补充 `subagent` skill 协作约定，明确当用户显式调用该技能时，默认启用多 agent 并行开发；同时保留主 agent 先锁边界、再并行分派的既有约束。
  - 影响文件或模块：`AGENTS.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：代码检查确认新增规则已写入“协作基本规则”与“推荐并行场景”两处，能与现有并行边界和接口冻结要求同时成立，未引入额外规则冲突。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：落实 M4 长期稳定性观察加固计划，补充规则主冒烟与正式 raw 样例的连续回合生命周期 / 离场触发链回归；新增“直到下个自己回合开始”的过期时点检查、多源结束主阶段延迟效果清理检查、跨完整回合的临时特殊登场许可过期检查，以及 `ON_LEAVE` 回手后的战斗清理稳定性检查，并修正 `RAID` 突进叠放断言此前被提前返回短路的问题。
  - 影响文件或模块：`docs/milestone_smoke_test.gd`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/mile_stone.md`、`docs/plan/development_plan.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 29 项通过、0 项失败；在沙箱外执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 41 项通过、0 项失败，输出 `CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`；在沙箱外执行 `res://docs/draw_phase_smoke_test.gd`，输出 `DRAW_PHASE_SMOKE_OK`。三次执行结束时仍存在既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：收紧 `card_preview_panel` 预览区默认尺寸，并让预览面板按顶部 HUD 与底部手牌区之间的可用高度自动缩放，避免预览区继续压到手牌区域。
  - 影响文件或模块：`ui/card_preview_panel.gd`、`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认预览区调整后手牌区域未与其重合；运行中仍有既有 Control anchor warning，但未影响本次布局验收通过。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：修正战场卡牌点击预览的触发时机，将对局输入门槛判断后的直接返回改为“先更新预览、再限制动作操作”，使点击对手前线与能量线卡牌时在非可操作态下也能正常进入预览。
  - 影响文件或模块：`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认脚本可正常加载且布局验收继续通过；另行执行 `--headless --quit` 时仍出现既有 signal 11 崩溃，未作为本次修复通过依据。
- 日期：2026-03-27
  - 变更类型：功能更新
  - 变更摘要：在 `card_preview_panel` 预览区补充独立的“提供能量”展示字段，使带原图的卡牌预览也能直接看到该卡可提供的能量信息，不再只在纯文字详情分支中显示。
  - 影响文件或模块：`ui/card_preview_panel.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认脚本可正常加载且布局验收继续通过；运行中仍有既有 Control anchor warning，但未影响本次展示改动验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：将战场卡牌点击信号补充为同时携带当前卡牌快照，并让 `battle_scene` 优先使用点击时传入的卡牌数据更新预览，避免对手战场卡牌点击时因二次反查失败而未能显示到预览面板。
  - 影响文件或模块：`ui/board_view.gd`、`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认信号签名调整后脚本可正常加载且布局验收继续通过；运行中仍有既有 Control anchor warning，但未影响本次修复验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：调整主界面手牌动作条的 `RAID` 按钮展示逻辑，为手牌快照补充 `special_play_rule`，并让选中的 `RAID` 卡在主阶段始终显示 `RAID` 按钮；当当前状态不满足发动条件时改为禁用并给出提示，避免“RAID 卡完全不显示按钮”造成误判。
  - 影响文件或模块：`core/game_manager.gd`、`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认脚本可正常加载，且动作条展示调整后手牌区域仍未遮挡战场区域；运行中仍有既有 Control anchor warning，但未影响本次修复验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：修正 `RAID` 底座名字校验只按原始卡名完全相等的问题，现已统一支持“这张卡也视为〈某名字〉”的正式卡文本；当正式 `RAID` 卡依赖这种“视为同名”关系寻找底座时，`available_actions` 会正确给出 `RAID`，按钮也会恢复可点击。
  - 影响文件或模块：`data/card_def.gd`、`core/rules_engine.gd`、`core/game_manager.gd`、`core/effect_resolver.gd`、`docs/hand_available_actions_smoke_test.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/hand_available_actions_smoke_test.gd`，结果为 16 项通过、0 项失败；新增正式回归“正式 RAID 卡可识别视为同名的底座”通过，确认 `UA31BT_MMM_1_071` 这类带“作为〈美樹 さやか〉处理”文本的底座已能被 `RAID` 正确识别。Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：修正 `battle_scene` 中 `RAID` 选底座模式的残留状态问题；当玩家改选其他手牌或战场卡时，会自动退出旧的 `RAID` 目标选择态，避免在条件已满足的情况下因为旧状态未清理而让 `RAID` 按钮继续保持置灰。
  - 影响文件或模块：`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认脚本可正常加载且动作条状态重置调整后手牌区域仍未遮挡战场区域；运行中仍有既有 Control anchor warning，但未影响本次修复验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：按实际规则语义纠正 `life_trigger_only` 的实现与验证口径：该字段现在只表示生命触发时出现“加手 / 立即 RAID”二选一，不再拦截 `RAID` 卡在主阶段满足能量、AP 与底座条件时从手牌直接 `RAID`；同时同步修正手牌动作冒烟、正式 raw 样例与里程碑文档中的旧预期。
  - 影响文件或模块：`core/rules_engine.gd`、`core/game_manager.gd`、`docs/hand_available_actions_smoke_test.gd`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/hand_available_actions_smoke_test.gd`，结果为 16 项通过、0 项失败，新增覆盖 `life_trigger_only=true` 与正式 `UA31BT_MMM_1_078` 在主阶段满足条件时仍会显示 `RAID`；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 41 项通过、0 项失败，确认正式 raw 样例中的主阶段手牌 `RAID`、生命触发 `RAID_NOW / ADD_TO_HAND` 与临时许可样例在新语义下同时成立；执行 `res://docs/milestone_smoke_test.gd`，结果为 29 项通过、0 项失败。三次运行结束时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：为战场卡牌点击与预览链路补充 `PreviewDebug` 调试日志，覆盖前线/能量线点击入口、预览设置与预览清空三个关键节点，便于继续定位“对方卡牌点击后未进入预览面板”的具体断点。
  - 影响文件或模块：`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认新增调试日志后脚本可正常加载且布局验收继续通过；运行中仍有既有 Control anchor warning，但未影响本次调试改动验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：修正 `PreviewDebug` 调试日志导致的递归栈溢出问题，将预览链路调试输出从 `append_ui_log()` 改为 Godot 控制台 `print()`，避免在 `state_changed -> 预览同步 -> 追加 UI 日志 -> 再次 state_changed` 之间形成无限递归。
  - 影响文件或模块：`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认递归修正后场景可正常加载；运行中仍有既有 Control anchor warning，但未影响本次修复验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：将 `PreviewDebug` 从仅输出到控制台改为由 `battle_scene` 本地拼接进日志面板显示，同时保持其不写回 `GameManager` 状态，便于直接在 UI 中观察对方卡牌点击与预览链路，而不再触发递归状态刷新。
  - 影响文件或模块：`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认日志面板拼接逻辑加入后脚本可正常加载且布局验收继续通过；运行中仍有既有 Control anchor warning，但未影响本次调试改动验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：修正战场点击事件可能被整屏 `BoardView` 根节点与非交互包装层拦截的问题，将 `board_view` 根节点、各区域 wrapper 与顶部统计标签统一改为 `MOUSE_FILTER_IGNORE`，让点击能够穿透到实际卡牌控件，便于继续验证对手卡牌预览链路。
  - 影响文件或模块：`ui/board_view.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认鼠标过滤调整后脚本可正常加载且布局验收继续通过；运行中仍有既有 Control anchor warning，但未影响本次修复验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：移除上一轮为排查对手卡牌预览问题而临时加入的 `PreviewDebug` 调试日志与日志面板拼接逻辑，恢复 `battle_scene` 的常规日志显示路径，仅保留已确认有效的点击穿透与预览行为修复。
  - 影响文件或模块：`ui/battle_scene.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 Godot `--resolution 1366x768 -- --layout-probe`，输出 `[PASS] UI 布局 1920x1080 (hand cards: 7)`，确认移除调试代码后脚本可正常加载且布局验收继续通过；运行中仍有既有 Control anchor warning，但未影响本次清理验证。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：修正 AI 处理生命区触发时仍可能把待决策暴露给人类 UI 的问题；为 `SimpleAI` 增加显式的生命触发决策分支，并在 `battle_scene` 中于 AI 持有优先权时隐藏 pending 决策下拉面板，避免 P1 直伤触发 AI 血区后弹出下拉框并卡住。
  - 影响文件或模块：`core/ai/simple_ai.gd`、`ui/battle_scene.gd`、`docs/simple_ai_v2_smoke_test.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/simple_ai_v2_smoke_test.gd`，结果为 7 项通过、0 项失败，新增覆盖“AI 默认会选择发动生命触发”；随后执行 `res://docs/vs_ai_smoke_test.gd`，结果为 2 项通过、0 项失败，确认 human vs ai 与 ai vs ai 驱动链均可继续自动推进。两次执行结束时仍存在既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：补强 AI 攻击链的可观测性，保持攻击结算仍在 `BattleResolver.resolve_attack()` 内横置攻击者，并将攻击日志从仅卡名改为“卡名 + 编号”；同时补充 AI 攻击专项回归，校验 AI 前线角色攻击后会进入 RESTED 且日志包含攻击卡牌名称与编号。
  - 影响文件或模块：`core/battle_resolver.gd`、`docs/simple_ai_v2_smoke_test.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/simple_ai_v2_smoke_test.gd`，结果为 8 项通过、0 项失败，新增覆盖“AI 攻击后攻击者进入 RESTED 且日志包含名称与编号”；随后执行 `res://docs/vs_ai_smoke_test.gd`，结果为 2 项通过、0 项失败，确认攻击日志增强未破坏 human vs ai 与 ai vs ai 的自动推进链。两次执行结束时仍存在既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
- 日期：2026-03-27
  - 变更类型：bugfix
  - 变更摘要：修正普通角色在对手回合完成阻挡后，会一直停留在 RESTED、未在自己回合开始恢复 ACTIVE 的问题；现已在起始阶段同时恢复当前回合玩家前线与能量线卡牌，并补充普通阻挡者跨回合恢复的专项回归。
  - 影响文件或模块：`core/turn_manager.gd`、`docs/milestone_smoke_test.gd`、`docs/logs/log_2026-03-27.md`
  - 验证方式与结果：在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 30 项通过、0 项失败，新增覆盖“普通阻挡者会在自己回合开始时恢复 ACTIVE”；同时执行 `res://docs/draw_phase_smoke_test.gd` 输出 `DRAW_PHASE_SMOKE_OK`。两次运行结束时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
