## 2026-04-17（P3 一致性守卫修复：强制增量路径重编）
- 变更类型：缺陷修复 / 编译门禁加固
- 变更摘要：修复 `tools/check_compile_incremental_consistency.py` 中“全量编译后直接增量编译”导致增量阶段被 `input_signature + 产物存在` 条件整体跳过的问题。新增 `_invalidate_series_outputs`，在全量快照采集后删除各系列 `cards_effects.json` 与 `cards_semantic.json`，确保第二次增量执行必须进入重编译路径，从而真实覆盖“复用 first-pass 缓存的增量重编译”一致性校验场景。
- 影响文件：
  - `tools/check_compile_incremental_consistency.py`
  - `tests/test_check_compile_incremental_consistency.py`
  - `docs/logs/log_2026-04-17.md`
- 验证方式：
  - `python -m unittest tests/test_check_compile_incremental_consistency.py`
  - `python tools/check_compile_incremental_consistency.py`
  - `python tools/check_utf8_docs.py`
- 结果：通过；一致性守卫现在会在增量轮次强制重建系列产物，能有效检测全量与增量重编路径的输出偏差。
- 后续事项：后续可补充“检查增量轮次 compile_stats 中 incremental_skipped_series=0”断言，进一步防止守卫退化为仅复读旧产物。

## 2026-04-17（P4 启动：模板冲突阻断与长期门禁自动化）
- 变更类型：计划推进 / 门禁策略冻结
- 变更摘要：`docs/plan/compile_cards_effects_optimization_plan.md` 正式进入 P4，新增阶段进度与执行口径，明确“冲突总量不高于基线 + 高优先级家族无新增冲突”的阻断策略，以及“本地快速门禁 + 合入前全量门禁”的长期自动化分层。
- 影响文件：
  - `docs/plan/compile_cards_effects_optimization_plan.md`
  - `docs/plan/development_plan.md`
  - `docs/logs/log_2026-04-17.md`
- 验证方式：
  - `python tools/check_utf8_docs.py`
  - `python -m unittest tests/test_compile_cards_effects.py`
- 结果：通过；P4 已进入执行态，且与项目主开发计划口径对齐。
- 后续事项：下一轮补齐“规则顺序漂移 + 冲突联动”联合告警，并将 family 维度趋势快照纳入周期化巡检。

## 2026-04-17（P4 完成：模板冲突阻断与长期门禁自动化落地）
- 变更类型：功能实现 / 门禁自动化
- 变更摘要：完成 P4 实施，`check_compiler_metrics_gate.py` 新增基线对比与冲突阻断策略（冲突增量、新冲突键、规则顺序漂移、高优先级 family 冲突回升）；`compile_cards_effects.py` 的冲突遥测新增 `candidate_families` 供 family 级门禁消费；`compiler_metrics_gate.json` 扩展为长期门禁配置；新增门禁脚本单元测试覆盖阻断与放行场景。
- 影响文件：
  - `tools/check_compiler_metrics_gate.py`
  - `tools/compile_cards_effects.py`
  - `docs/plan/compiler_metrics_gate.json`
  - `docs/plan/compiler_metrics_snapshot.json`
  - `tests/test_check_compiler_metrics_gate.py`
  - `docs/plan/compile_cards_effects_optimization_plan.md`
  - `docs/plan/development_plan.md`
  - `docs/logs/log_2026-04-17.md`
- 验证方式：
  - `python -m unittest tests/test_check_compiler_metrics_gate.py`
  - `python -m unittest tests/test_compile_cards_effects.py`
  - `python tools/check_compiler_metrics_gate.py`
  - `python tools/check_utf8_docs.py`
- 结果：通过；P4 门禁能力已具备可回归、可配置、可阻断的长期治理能力。
- 后续事项：后续新增模板族时按门禁配置维护流程同步更新 `required_family_hits` 与 `protected_families_no_new_conflicts`，避免门禁漂移。

## 2026-04-17（CGD 卡图补录口径对齐：文件名归一化与文档同步）
- 变更类型：缺陷修复 / 文档对齐
- 变更摘要：针对 `pic/CGD` 中大量带 `_1`、`_2` 的同号卡图，补充导入脚本的文件名归一化逻辑：在映射官网卡号前先剥离末尾补档序号后缀，避免把 `UA34BT-CGD-1-033_2` 误当成官网卡号。同步更新 `data/cards/parser.md`，明确 CGD 目录下同号多图的导入预期与 `-RefreshExisting` 使用口径。
- 影响文件：
  - `tools/import_cards_raw_from_pic.ps1`
  - `data/cards/parser.md`
  - `docs/logs/log_2026-04-17.md`
- 验证方式：
  - `python - <<'PY' ...`（统计 `pic/CGD` 文件名中 `_数字` 后缀分布与归一化唯一编号数）
  - `python tools/check_utf8_docs.py`
- 结果：通过；CGD 同号补档文件可复用同一官网卡号导入口径，文档与脚本实现保持一致。
- 后续事项：后续若出现非 `_数字` 形式的补档后缀，再扩展归一化规则并补充相应样例验证。

## 2026-04-17（CGD 实卡导入：按 parser 文档口径补录 cards_raw）
- 变更类型：数据补录
- 变更摘要：按 `data/cards/parser.md` 既定口径对 `pic/CGD` 顶层卡图执行导入。导入结果：扫描 100 张、跳过同号补档 21 张（`_1/_2`）、新增 79 张、失败 0；生成 `data/cards/CGD/cards_raw.json`。
- 影响文件：
  - `data/cards/CGD/cards_raw.json`
  - `docs/logs/log_2026-04-17.md`
- 验证方式：
  - `python - <<'PY' ...`（导入执行并输出 `scanned/skipped_existing/added/failed`）
  - `python - <<'PY' ...`（校验 `cards_raw` 条目数、首尾编号与重复编号）
  - `python tools/check_utf8_docs.py`
- 结果：通过；`CGD` 系列 `cards_raw` 已按文档规则补录完成，编号区间覆盖 `UA34BT/CGD-1-001` 至 `UA34BT/CGD-1-080`，无重复编号。
- 后续事项：如需下游产物，可继续执行 `tools/compile_cards_effects.py` 生成 `cards_effects.json` 与 `cards_semantic.json`。

## 2026-04-17（CGD 下游同步：effects/semantic 编译产物落盘）
- 变更类型：下游同步
- 变更摘要：执行 `python tools/compile_cards_effects.py` 完成下游同步，新增 `data/cards/CGD/cards_effects.json` 与 `data/cards/CGD/cards_semantic.json`，并刷新编译指标快照 `docs/plan/compiler_metrics_snapshot.json`。
- 影响文件：
  - `data/cards/CGD/cards_effects.json`
  - `data/cards/CGD/cards_semantic.json`
  - `docs/plan/compiler_metrics_snapshot.json`
  - `docs/logs/log_2026-04-17.md`
- 验证方式：
  - `python tools/compile_cards_effects.py`
  - `python tools/check_utf8_docs.py`
- 结果：通过；编译日志显示已覆盖 5 个系列目录并写出 CGD 下游产物。
- 后续事项：如需补齐 `pic/CGD/micro/` 缩略图，请在具备 PowerShell 环境时执行 `tools/generate_micro_card_images.ps1`。

## 2026-04-17（parser 文档补充：新系列解析与编译闭环流程）
- 变更类型：文档完善 / 流程标准化
- 变更摘要：在 `data/cards/parser.md` 新增“新系列解析与编译闭环流程（支持矩阵 + 开发计划）”章节，固化每次新系列导入后的固定流水线（编译、支持率统计、原子复用矩阵、未支持文本族聚类、快照与 budget 门禁）及开发计划模板，确保后续迭代按“文本族 + 原子能力”收敛，不回退到按卡修补。
- 影响文件：
  - `data/cards/parser.md`
  - `docs/logs/log_2026-04-17.md`
- 验证方式：
  - `python tools/check_utf8_docs.py`
  - `python tools/report_support_stats.py --cards-root data/cards`
- 结果：通过；文档章节已补齐，UTF-8 校验通过，支持率统计脚本可正常执行。
- 后续事项：后续每次导入新系列时按该章节命令清单执行，并同步更新 `docs/plan/<series>_onboarding_plan.md`。
