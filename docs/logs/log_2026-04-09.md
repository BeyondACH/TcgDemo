## 2026-04-09
- 变更类型：计划文档中文化
- 变更摘要：将 `docs/plan/todo.md` 从英文专项计划文档完整转换为中文版本，保留原有结构、模板族命名、契约字段与阶段划分，同时统一中文说明口径，便于后续直接按仓库中文文档规范继续维护与执行。
- 影响文件：`docs/plan/todo.md`、`docs/logs/log_2026-04-09.md`
- 验证方式：使用显式 UTF-8 重读 `docs/plan/todo.md` 与 `docs/logs/log_2026-04-09.md`；执行 `git diff -- docs/plan/todo.md docs/logs/log_2026-04-09.md`
- 结果：`todo.md` 已完成中文化，模板 ID、字段名与验收项保持原契约不变，中文文档编码未发现异常。
- 后续事项：若后续继续扩充该专项计划，应继续保持“中文说明 + 英文字段/枚举/模板名”的写法，避免再次出现中英混杂且缺少统一口径的状态。

- 变更类型：计划文档重构
- 变更摘要：将 `docs/plan/todo.md` 从零散待办清单重构为“编译器原子能力通用识别改造方案与执行 TODO”一体版文档，补齐文档定位、目标/非目标、目标架构、契约冻结、模板与 override 边界、分阶段实施计划、测试矩阵、并行边界、风险与回退策略，使其既能解释识别层重构方案，也能直接指导实施与验收。
- 影响文件：`docs/plan/todo.md`、`docs/logs/log_2026-04-09.md`
- 验证方式：使用显式 UTF-8 重读 `docs/plan/todo.md` 与 `docs/logs/log_2026-04-09.md`；执行 `git diff -- docs/plan/todo.md docs/logs/log_2026-04-09.md`
- 结果：文档已按 UTF-8 安全流程改写，方案结构已与当前 `development_plan.md` 和 `rule.md` 口径对齐，可作为后续编译器识别层重构的专项执行文档。
- 后续事项：若后续真正启动 `compile_cards_effects.py` 的识别层重构，应先按本文档冻结 semantic/effects 契约，再拆分编译主链、测试与文档 sidecar 任务。

- 变更类型：工具脚本增强
- 变更摘要：更新 `download_mmm_images.py`，保留日文 cardlist 页面 `series` 下拉框商品解析，并将 `--list-products` 从只读列表入口调整为交互式入口：先输出带序号的商品名列表，再提示输入序号，并把选中的商品名带回既有下载主流程；同时补齐对应测试。
- 影响文件：`download_mmm_images.py`、`tests/test_download_mmm_images.py`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-09.md`
- 验证方式：执行 `python -m unittest tests.test_download_mmm_images`；执行 `python download_mmm_images.py --help`
- 结果：7 项测试通过，`--help` 可正常输出 `--list-products`；列表模式会输出序号并在输入合法序号后继续进入下载流程，非法序号会直接报错退出。
- 后续事项：如需再补“按作品先筛商品名”联动，应继续以日文 cardlist 页面为唯一来源，并在不破坏现有 `--product` 直传语义的前提下另轮设计。

- 变更类型：工具脚本调整
- 变更摘要：确认 live `weblist` 接口不支持按官网 `selectTitle` 作品标题直接过滤后，撤回 `--list-title` 方案；移除对应 CLI 入口、交互分支和仅服务该方案的测试，保留 `--list-products` 为唯一可信的交互筛选入口，同时继续保留从官网标题下拉框读取标题列表的兼容能力，以供商品名反推作品名流程使用。
- 影响文件：`download_mmm_images.py`、`tests/test_download_mmm_images.py`、`docs/logs/log_2026-04-09.md`
- 验证方式：执行 `python -m unittest tests.test_download_mmm_images`；执行 `python download_mmm_images.py --help`
- 结果：回归测试通过，`--help` 不再输出 `--list-title`，传入该参数时会按未知参数处理；`--list-products` 现有交互下载流程保持不变。
- 后续事项：若未来需要“按作品先选、再按产品下载”，应单独设计为“标题仅用于筛产品列表”，不要再把标题选择伪装成可直接下载过滤。

- 变更类型：工具脚本调整
- 变更摘要：将 `build_source_url()` 收敛为参考 live `weblist?good=...` 形式的简化拼接方式，下载链接只保留可靠的 `good` 产品过滤项以及分页参数，不再携带一串空的 `works`、`color` 等筛选字段；同时同步更新对应测试断言。
- 影响文件：`download_mmm_images.py`、`tests/test_download_mmm_images.py`、`docs/logs/log_2026-04-09.md`
- 验证方式：执行 `python -m unittest tests.test_download_mmm_images`；执行 `python download_mmm_images.py --help`
- 结果：13 项测试通过；构造出的下载链接已改为以 `good=` 为核心的简化形式，`--list-products` 现有流程保持可用。
- 后续事项：若后续确认 `weblist` 对分页参数也可省略，可再单独评估是否继续压缩 URL 形态。

- 变更类型：工具脚本重构
- 变更摘要：移除 `OFFICIAL_JP_CARDLIST_URL` 及其 HTML 解析链路，统一改为从 `attrweblist` 读取结构化筛选数据：作品列表改读 `data.works`，产品列表改读 `data.goods`；同步删除只服务官网页面的解析逻辑与对应测试，并将 `--list-products` 的数据来源切换到 `attrweblist`。
- 影响文件：`download_mmm_images.py`、`tests/test_download_mmm_images.py`、`docs/logs/log_2026-04-09.md`
- 验证方式：执行 `python -m unittest tests.test_download_mmm_images`；只读请求 `https://uapcapi.windoent.com/card/card/attrweblist`
- 结果：13 项测试通过；线上 `attrweblist` 可成功返回 `works` 与 `goods`，脚本不再依赖官网 HTML 页面。
- 后续事项：若后续发现个别 `goods` 名称无法稳定反推 `works`，应单独补一层显式映射，不再回退到页面解析。
- 变更类型：工具脚本调整
- 变更摘要：更新 `download_images.py` 默认下载目录逻辑，图片现在默认存放到 `pic/作品名称`，不再把 `color` 信息拼接进目录名；同步补充针对默认输出目录的回归测试。
- 影响文件：`download_images.py`、`tests/test_download_images.py`、`docs/logs/log_2026-04-09.md`
- 验证方式：执行 `python -m unittest tests.test_download_images`；执行 `python download_images.py --help`
- 结果：回归测试通过，帮助文案已更新为 `pic/<work>`，默认下载路径不再包含颜色后缀。
- 后续事项：若后续确认 `--color` 参数也不再需要，可再单独收敛 CLI 参数与相关提示。

- 变更类型：卡图目录切换
- 变更摘要：将运行时卡图读取切换为按 `title_code` 分目录读取，统一使用 `pic/<title_code>/<image>` 与 `pic/<title_code>/micro/<image>`；`SnapshotSerializer` 现已向 UI 快照补充 `title_code`，`CardView` 与 `CardPreviewPanel` 不再读取旧的平铺 `pic/` / `pic/micro/` 路径；同步更新 `download_images.py` 默认输出目录与 `tools/generate_micro_card_images.ps1` 的系列目录扫描逻辑。
- 影响文件：`core/ui/snapshot_serializer.gd`、`ui/card_view.gd`、`ui/card_preview_panel.gd`、`download_images.py`、`tools/generate_micro_card_images.ps1`、`tests/test_download_images.py`、`tests/test_generate_micro_card_images.py`、`docs/card_image_path_smoke_test.gd`、`docs/logs/log_2026-04-09.md`
- 验证方式：执行 `python -m unittest tests.test_download_images`；执行 `python -m unittest tests.test_generate_micro_card_images`；执行 `Godot\\Godot_v4.6.1-stable_win64_console.exe --headless --path D:\\CodexWork\\TcgDemo --script res://docs/card_image_path_smoke_test.gd`
- 结果：Python 回归测试与新增 Godot smoke test 均通过；可见卡牌快照已包含 `title_code`，手牌/战场/预览的候选路径已全部切换到系列目录，缩略图脚本也已改为输出到对应作品目录下的 `micro` 子目录。
- 后续事项：若后续继续补录 `TLR` 等新系列卡图，应顺带执行缩略图脚本补齐 `pic/<title_code>/micro/`，并继续避免恢复旧的平铺资源路径。

- 变更类型：卡图补录与工具修复
- 变更摘要：修复 `tools/import_cards_raw_from_pic.ps1`，使其支持扫描 `pic/<title_code>/` 顶层卡图目录，并补充 `tests/test_import_cards_raw_from_pic.py` 回归测试；随后完成 `pic/TLR/` 全量卡图补录，同步生成 `data/cards/TLR/cards_raw.json`、`pic/TLR/micro/` 缩略图以及 `data/cards/TLR/cards_effects.json`、`data/cards/TLR/cards_semantic.json`。
- 影响文件：`tools/import_cards_raw_from_pic.ps1`、`tests/test_import_cards_raw_from_pic.py`、`tests/test_generate_micro_card_images.py`、`data/cards/TLR/cards_raw.json`、`data/cards/TLR/cards_effects.json`、`data/cards/TLR/cards_semantic.json`、`pic/TLR/micro/*`、`docs/superpowers/plans/2026-04-09-tlr-import-sync.md`、`docs/logs/log_2026-04-09.md`
- 验证方式：执行 `python -m unittest tests.test_import_cards_raw_from_pic tests.test_generate_micro_card_images -v`；沙箱外执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\tools\\import_cards_raw_from_pic.ps1`；执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\tools\\generate_micro_card_images.ps1`；执行 `python tools/compile_cards_effects.py`
- 结果：目录扫描测试通过；导入脚本共扫描 199 张卡图，跳过已存在 117 张，新增导入 `TLR` 82 张，失败 0；`pic/TLR/micro/` 生成 82 张缩略图；`TLR` 的 `cards_raw.json`、`cards_effects.json`、`cards_semantic.json` 均为 82 条；编译汇总显示全仓库运行时支持能力 263、未支持能力 50，其中 `TLR` 系列编译结果未发现未支持能力。
- 后续事项：如后续继续补新系列，保持沿用 `pic/<title_code>/` 目录结构与本轮导入、缩略图、编译顺序；全仓库剩余 50 条未支持能力可在后续单独按模板族收敛处理。

- 变更类型：工具脚本增强
- 变更摘要：为 `download_images.py` 增加 `--list-works` 交互入口；脚本会先输出官方作品列表并提示输入编号，选中后直接使用 `weblist?works=<作品名>` 下载该作品下全部产品的卡图；同时保留原有 `--list-products` 与 `--product` 下载入口不变。
- 影响文件：`download_images.py`、`tests/test_download_images.py`、`docs/plan/development_plan.md`、`docs/logs/log_2026-04-09.md`、`docs/superpowers/specs/2026-04-09-download-images-list-works-design.md`、`docs/superpowers/plans/2026-04-09-download-images-list-works.md`
- 验证方式：执行 `python -m unittest tests.test_download_images -v`
- 结果：5 项测试通过；`build_source_url()` 在作品模式下会输出 `works=` 且不再附带 `good=`，`--help` 已包含 `--list-works`，交互选择作品后会直接进入下载主流程。
- 后续事项：若后续需要“先选作品再只下载其中某个产品”，应单独新增二段式交互，不要回退当前按作品下载全部产品的入口语义。

- 变更类型：方案设计
- 变更摘要：围绕 `docs/plan/todo.md` 补充编译器模板通用化重构设计文档，明确渐进式双轨迁移路线、模板层与 override 层边界、semantic/effects 英文契约、`MMM -> TLR` 迁移顺序、并行拆分与验证门槛，为后续正式编写实施计划提供统一基线。
- 影响文件：`docs/superpowers/specs/2026-04-09-compiler-template-generalization-design.md`、`docs/logs/log_2026-04-09.md`
- 验证方式：使用显式 UTF-8 重读新增 spec 与日志；执行 `git diff -- docs/superpowers/specs/2026-04-09-compiler-template-generalization-design.md docs/logs/log_2026-04-09.md`
- 结果：设计文档已落盘，内容与 `docs/plan/todo.md`、`docs/plan/development_plan.md`、`docs/rules/rule.md` 口径一致，能够直接作为下一步实施计划的输入。
- 后续事项：待确认 spec 无需再调整后，再继续展开为任务级 implementation plan。

- 变更类型：方案设计
- 变更摘要：基于已确认的编译器模板通用化 spec，补充任务级 implementation plan，细化到模块拆分、测试入口、`MMM -> TLR` 迁移顺序、legacy 清理节点与每个阶段的提交/验证命令，供后续按任务逐项执行。
- 影响文件：`docs/superpowers/plans/2026-04-09-compiler-template-generalization.md`、`docs/logs/log_2026-04-09.md`
- 验证方式：使用显式 UTF-8 重读新增 plan 与日志；执行 `git diff -- docs/superpowers/plans/2026-04-09-compiler-template-generalization.md docs/logs/log_2026-04-09.md`
- 结果：实施计划已落盘，任务粒度与验证口径已对齐专项 spec，可直接进入执行模式。
- 后续事项：若下一步开始实施，推荐优先按 Task 1-2 建立测试与抽取 seam，再进入 MMM/TLR 迁移，避免在无回归保护下直接清理 legacy。
