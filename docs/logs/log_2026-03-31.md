# 2026-03-31

## 日志条目 1

- 日期：2026-03-31
- 变更类型：bugfix
- 变更摘要：修复生命触发 `RAID` 在点击 Activate 后当前不满足条件时会卡住的问题；现在会统一回退为加入手牌，并在没有其他待处理生命链时正确完成生命伤害收尾。同时补充规则主冒烟与正式 raw 冒烟回归，覆盖”生命触发 RAID 失败自动回手”。
- 影响文件或模块：`core/game_manager.gd`、`docs/milestone_smoke_test.gd`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`README.md`、`docs/logs/log_2026-03-31.md`
- 验证方式与结果：执行 `res://docs/milestone_smoke_test.gd`，结果为 `33` 项通过、`0` 项失败，新增通过”RAID 生命触发不满足条件时自动加入手牌”；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果先为 `68` 项通过、`1` 项失败，新增通过”Raw Life Trigger Raid Falls Back To Hand When Illegal”，随后修正 `Raw Battle Scene Resolves P1 Life Trigger Board Target Click` 用例中与正式卡目标方语义不一致的旧断言并复跑，最终结果为 `69` 项通过、`0` 项失败，输出 `CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`。运行结束时仍存在既有 `ObjectDB instances leaked at exit` 与 `resources still in use at exit` 告警，但未影响本次新增规则断言通过。

## 日志条目 2

- 日期：2026-03-31
- 变更类型：refactor
- 变更摘要：GameManager 架构重构，从”上帝对象”转为协调器模式，遵循 SOLID 单一职责原则。提取 7 个专用管理器，移除 130 行重复代码，GameManager 行数从 1467 行减至 1060 行。
- 影响文件或模块：
  - 新增文件：
    - `core/player_utils.gd` - 共享助手 (`opponent_of`)
    - `core/game_gate_checker.gd` - 状态门检查
    - `core/deck_loader.gd` - 卡组/卡牌加载
    - `core/ui/snapshot_serializer.gd` - UI 序列化
    - `core/decision_manager.gd` - 决策队列管理
    - `core/life_trigger_manager.gd` - 生命触发/揭示
    - `core/controllers/controller_manager.gd` - 控制器管理
  - 修改文件：
    - `core/game_manager.gd` - 精简为协调器
    - `core/rules_engine.gd` - 新增 `get_energy_pool_for_player()` public API
    - `core/victory_checker.gd` - 使用 `PlayerUtils.opponent_of()`
    - `core/battle_resolver.gd` - 使用 `PlayerUtils.opponent_of()`
    - `core/effect_resolver.gd` - 使用 `PlayerUtils.opponent_of()`
    - `core/effects/requirement_matcher.gd` - 使用 `PlayerUtils.opponent_of()`
    - `core/effects/target_selector.gd` - 使用 `PlayerUtils.opponent_of()`
- 移除重复代码：
  - `_opponent_of()` - 6 处重复合并为 1 处共享
  - `_energy_pool_for_player()` - 80 行重复代码删除
  - `_available_actions_for_card()` - 50 行重复代码删除
- 验证方式与结果：执行 `res://docs/milestone_smoke_test.gd`，结果为 `33` 项通过、`0` 项失败。所有规则语义保持不变，API 兼容性验证通过。
