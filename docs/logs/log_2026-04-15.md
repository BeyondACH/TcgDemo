## 2026-04-15
- 变更类型：能力扩展 / KGD 未支持能力收敛（阶段推进）
- 变更摘要：针对 KGD 系列 `ON_PLAY` 未支持能力补齐第一批可复用实现，新增“黄卡特征种类计数”需求类型，并在 `compile_cards_effects` 的事件编译链补充 6 条文本模板落地：条件抽牌、指定名称检索入手（含异名约束）、按特征动态 BP 退场、条件赋予 `SNIPER` 与不可阻挡占位关键词、以及“王贲在场时回手改退场”分支规则。
- 影响文件：
  - `core/effects/requirement_matcher.gd`
  - `tools/compile_cards_effects.py`
  - `data/cards/KGD/cards_effects.json`
  - `data/cards/KGD/cards_semantic.json`
- 验证方式：
  - `python tools/compile_cards_effects.py`
  - `python -m unittest tests/test_compile_cards_effects.py`
  - 脚本复核 `data/cards/KGD/cards_effects.json` 中 `status == UNSUPPORTED` 的剩余条目
- 结果：通过；总未支持能力由 `13` 降至 `7`，运行时汇总更新为 `Supported=552 / Unsupported=7`。KGD 当前剩余未支持集中在复合分支与条件多段结算文本（`030`、`033`、`064`、`097`、`100`）。
- 后续事项：继续按“先可复用模板、后卡牌接入”的原则收口剩余 7 条，优先处理 `064` 的“抽牌 + 二选一分支”组合模板与 `100` 的条件数量上限召唤模板。

## 2026-04-15（KGD 未支持能力收口完成）
- 变更类型：能力扩展 / KGD `UNSUPPORTED` 归零
- 变更摘要：继续补齐 KGD 剩余 7 条未支持能力，新增 `PREVIEW_BOTTOM_DECK` 运行时步骤、`CONTROLLER_HAS_TRAIT_BP_GTE` 需求，以及“角色效果导致退场”回合标记；在事件编译链落地 `030`、`033`、`064`（含抽牌后二选一分支与两条子分支文本）、`097`、`100` 的可执行编译结果，并修正带分支锚点的项目符号效果重复编译问题。
- 影响文件：
  - `core/effects/step_executor.gd`
  - `core/effects/requirement_matcher.gd`
  - `tools/compile_cards_effects.py`
  - `data/cards/KGD/cards_effects.json`
  - `data/cards/KGD/cards_semantic.json`
- 验证方式：
  - `python tools/compile_cards_effects.py`
  - `python -m unittest tests/test_compile_cards_effects.py`
  - 脚本统计 `data/cards/KGD/cards_effects.json` 中 `UNSUPPORTED` 数量
- 结果：通过；运行时汇总更新为 `Supported=557 / Unsupported=0`，KGD 系列 `UNSUPPORTED` 为 `0`。
- 后续事项：后续优先把本轮“占位关键词”和“条件分支简化处理”收敛为更稳定的原子模板族，并补充对应流程回归。


## 2026-04-15（原子类型候选计划补充）
- 变更类型：计划文档更新
- 变更摘要：新增 `docs/plan/atomic_type_candidates_plan.md`，整理“新增原子类型候选清单（type 名称 + 输入字段 + 示例迁移卡）”，并按 P0~P3 优先级拆分为可落地任务单与里程碑。
- 影响文件：
  - `docs/plan/atomic_type_candidates_plan.md`
  - `docs/logs/log_2026-04-15.md`
- 验证方式：
  - 显式 UTF-8 读取两份文档
  - `python tools/check_utf8_docs.py`
- 结果：通过；文档编码正常，计划内容可直接用于后续实现排期。
- 后续事项：按计划先落地 P0（`PENDING_BRANCH_EFFECT` 收敛）并同步补流程级回归。

## 2026-04-15（M1/P0 首批分支占位收敛）
- 变更类型：规则能力扩展 / 分支步骤原子化
- 变更摘要：实现 `APPLY_BRANCH_EFFECT_PRESET` 运行时步骤，并在编译链把分支占位文本收敛为首批 3 个 preset：`UNSELECTABLE_BY_OPPONENT_EFFECT_THIS_TURN`、`CANNOT_BLOCK_NAME_CONTAINS_THIS_TURN`、`BP_THRESHOLD_OVERRIDE_IF_NAME_PRESENT`。同步接入阻挡校验中的“名称包含限制”运行时关键词判定，迁移 `UA45BT_TLR_1_055_on_enter_536516f54f` 与 `UA36BT_MCR_1_065_on_play_78e3e36e0b` 的分支占位步骤。
- 影响文件：
  - `core/effects/step_executor.gd`
  - `core/rules_engine.gd`
  - `tools/compile_cards_effects.py`
  - `tests/test_compile_cards_effects.py`
  - `data/cards/TLR/cards_effects.json`
  - `data/cards/MCR/cards_effects.json`
  - `docs/plan/atomic_type_candidates_plan.md`
- 验证方式：
  - `python tools/compile_cards_effects.py`
  - `python -m unittest tests/test_compile_cards_effects.py`
  - 脚本统计全量 `cards_effects.json` 中 `PENDING_BRANCH_EFFECT` 余量
- 结果：通过；已完成 M1/P0 首批 3 个 preset 接入，`PENDING_BRANCH_EFFECT` 由 `8` 降至 `5`。
- 后续事项：继续按 M1 迁移清单收敛剩余 5 条分支占位文本，并补最小对局级流程回归。

## 2026-04-15（M1/P0 收口完成）
- 变更类型：规则能力扩展 / 分支占位清零
- 变更摘要：继续扩展 `_compile_branch_sub_steps`，补齐剩余 5 条分支文本模板（场外回手、手牌登场、可选弃牌后激活、看牌检索、指定名称加攻后抽牌），完成 `PENDING_BRANCH_EFFECT` 的全量收敛；同步补充编译测试，确保上述分支不再回退到占位步骤。
- 影响文件：
  - `tools/compile_cards_effects.py`
  - `tests/test_compile_cards_effects.py`
  - `data/cards/TLR/cards_effects.json`
  - `docs/plan/atomic_type_candidates_plan.md`
  - `docs/logs/log_2026-04-15.md`
- 验证方式：
  - `python tools/compile_cards_effects.py`
  - `python -m unittest tests/test_compile_cards_effects.py`
  - 脚本统计全量 `cards_effects.json` 中 `PENDING_BRANCH_EFFECT` 数量
- 结果：通过；`PENDING_BRANCH_EFFECT` 由 `5` 降至 `0`，M1/P0 完成。
- 后续事项：进入 M2（P1）高频组合流程原子沉淀，并补对应流程级回归。

## 2026-04-15（M2/P1 高频组合流程原子落地）
- 变更类型：规则能力扩展 / 复合步骤原子化
- 变更摘要：在 `StepExecutor` 新增 `SELECT_MOVE_WITH_FALLBACK` 与 `SELECT_AND_PLAY_BY_PROFILE`，并在编译链完成两张示例卡迁移：`UA45BT_TLR_1_002_on_enter_43348ae670` 收敛“主动作失败走后备动作”样板链，`UA31BT_MMM_1_001_on_leave_b0a2383fc6` 收敛“按画像选择后登场”样板链。
- 影响文件：
  - `core/effects/step_executor.gd`
  - `tools/compile_cards_effects.py`
  - `tests/test_compile_cards_effects.py`
  - `data/cards/TLR/cards_effects.json`
  - `data/cards/MMM/cards_effects.json`
  - `docs/plan/atomic_type_candidates_plan.md`
  - `docs/logs/log_2026-04-15.md`
- 验证方式：
  - `python tools/compile_cards_effects.py`
  - `python -m unittest tests/test_compile_cards_effects.py`
  - 脚本抽样核对迁移能力步骤类型（TLR `002` / MMM `001`）
- 结果：通过；M2/P1 两个目标原子均已接入运行时与编译产物，迁移卡不再依赖旧样板链。
- 后续事项：进入 M3（P2），优先实现 `REFILL_LIFE_IF_EMPTY` 与 `REGISTER_NEXT_PLAY_COST_MODIFIER`。

## 2026-04-15（M3/P2 优先项原子落地）
- 变更类型：规则能力扩展 / 复合步骤原子化
- 变更摘要：在 `StepExecutor` 新增 `REFILL_LIFE_IF_EMPTY` 与 `REGISTER_NEXT_PLAY_COST_MODIFIER`。编译链已迁移两类优先模板：
  1) “生命为空时从牌库顶补 1 命”改为 `REFILL_LIFE_IF_EMPTY`；
  2) “注册下次出牌费用变更（AP -1）”改为 `REGISTER_NEXT_PLAY_COST_MODIFIER`。
- 影响文件：
  - `core/effects/step_executor.gd`
  - `tools/compile_cards_effects.py`
  - `tests/test_compile_cards_effects.py`
  - `data/cards/KGD/cards_effects.json`
  - `data/cards/MMM/cards_effects.json`
  - `data/cards/TLR/cards_effects.json`
  - `data/cards/KGD/cards_semantic.json`
  - `data/cards/MMM/cards_semantic.json`
  - `data/cards/TLR/cards_semantic.json`
  - `docs/plan/atomic_type_candidates_plan.md`
  - `docs/logs/log_2026-04-15.md`
- 验证方式：
  - `python tools/compile_cards_effects.py`
  - `python -m unittest tests/test_compile_cards_effects.py`
  - 脚本抽样核对迁移能力步骤类型与字段
- 结果：通过；M3/P2 优先项两类原子均已接入运行时并产出到编译结果，`Supported=557 / Unsupported=0` 维持不变。
- 后续事项：继续 M3 剩余项（含 `cost_delta.energy` 更广覆盖与边界回归），并推进 M4 高级选择器约束扩展。

## 2026-04-15（PR#40 Codex Review P1 缺陷修复）
- 变更类型：缺陷修复 / 效果队列续跑
- 变更摘要：修复 `SELECT_MOVE_WITH_FALLBACK` 与 `SELECT_AND_PLAY_BY_PROFILE` 在手动选目标分支下的续跑丢失问题。两类原子在进入 `_step_select_targets` 时不再直接透传外层 `remaining_steps`，而是构建“当前原子步骤 + 外层后续步骤”的内部 continuation，确保玩家完成选择后会先回到当前原子执行移动/后备动作或登场步骤，再继续后续 effect 步骤。
- 影响文件：
  - `core/effects/step_executor.gd`
  - `docs/logs/log_2026-04-15.md`
- 验证方式：
  - `python -m unittest tests/test_compile_cards_effects.py`
  - 人工代码复核 `TARGET_SELECTION -> resolve_target_selection_decision` 续跑链路
- 结果：通过；手动目标选择恢复后不再跳过原子后处理步骤。
- 后续事项：补充覆盖手动选择暂停/恢复链路的运行时测试（含 fallback 与 play-profile 两类原子）。


## 2026-04-15（M3/P2 剩余项收口：cost_delta.energy 与边界回归）
- 变更类型：缺陷修复 / 规则原子能力补强 / 边界回归
- 变更摘要：
  1) `REGISTER_NEXT_PLAY_COST_MODIFIER` 的 `cost_delta.energy` 改为纳入 delayed-effect 一次性消费链，新增 `MODIFY_PLAY_COST_ENERGY` 并与 `once=true` 对齐；
  2) 修复仅配置 `cost_delta.energy` 时会提前返回、导致下次出牌减费不生效的问题；
  3) 编译链新增“下次从 REMOVED 使用时必要エナジー减免”模板，并在缺失能量标签时返回 `MISSING_SOURCE_LABEL_ENERGY_DELTA`；
  4) 回归补齐“减费下限不低于 0 / 未声明颜色不变 / once 消费后恢复原值”。
- 影响文件：
  - `core/effects/step_executor.gd`
  - `core/effect_resolver.gd`
  - `tools/compile_cards_effects.py`
  - `tests/test_compile_cards_effects.py`
  - `docs/runtime_residue_smoke_test.gd`
  - `docs/plan/atomic_type_candidates_plan.md`
  - `docs/logs/log_2026-04-15.md`
- 验证方式：
  - `python -m unittest tests/test_compile_cards_effects.py`
  - `python tools/compile_cards_effects.py`
- 结果：通过；`Supported=557 / Unsupported=0` 保持不变，M3 剩余项已收口。
- 后续事项：推进 M4 `SELECT_TARGETS_BY_COMBINATION` 约束协议扩展，并补对应流程回归。

## 2026-04-15（编译链去按卡硬编码：场上名称二选一使用条件）
- 变更类型：编译器重构 / 可复用规则收敛
- 变更摘要：将 `compile_cards_effects.py` 中“自分の場に〈A〉か〈B〉がある場合のみ使用できる”从 `UA31BT_MMM_1_028` 的按卡硬编码提炼为通用解析函数 `_parse_field_name_or_requirements`，并同时接入 `_build_play_requirements` 与事件能力编译分支，避免同类文本继续依赖卡号特判。
- 影响文件：
  - `tools/compile_cards_effects.py`
  - `tests/test_compile_cards_effects.py`
  - `docs/logs/log_2026-04-15.md`
- 验证方式：
  - `python -m unittest tests/test_compile_cards_effects.py`
  - `python tools/compile_cards_effects.py`
- 结果：通过；新增用例覆盖“非指定卡号下的二选一名称使用条件”在事件编译与出牌规则构建两条链路的产物一致性。
- 后续事项：后续可按同一思路继续抽象“前线/场上 + 多名称 OR 条件”族，逐步减少历史特判分支。
