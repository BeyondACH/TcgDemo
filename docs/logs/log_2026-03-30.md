# 2026-03-30

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：根据 `data/cards/cards_effects.json` 与 `data/cards/cards_semantic.json` 的当前实际产物，重写 `docs/plan/card_design.md`，将其从“76 个已支持、0 个未支持”的过期状态更新为“99 张卡、104 个已支持能力、25 个未支持能力、分布于 19 张卡”的待办清单，并按可复用的 requirement / target / step / play rule 缺口重新归类，补充卡牌到能力缺口映射与推荐实施顺序。
- 影响文件或模块：`docs/plan/card_design.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：使用显式 UTF-8 读取 `docs/rules/rule.md`、`docs/plan/development_plan.md`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json` 与最近日志；静态统计确认 `cards_effects.json` 当前为 `99` 张卡、`104` 个已支持能力、`25` 个未支持能力、`19` 张卡存在未支持项，且未支持时机分布为 `ON_ENTER 9 / ON_PLAY 6 / MAIN_ACTIVATE 5 / ON_ATTACK 2 / ON_LIFE_TRIGGER 2 / ON_LEAVE 1`；随后复查 `docs/plan/card_design.md`，确认文档内容已与当前编译产物一致。本轮仅更新计划文档与日志，未改动运行时代码，因此未执行 Godot 冒烟测试。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：落实第一批未支持原子能力模板，新增 `OUTSIDE -> FRONT_LINE` 过滤登场、`PREVIEW_TOP_DECK` 检索/重排/余牌去向、以及 `HAND / 预览上下文 -> FRONT_LINE` 过滤登场三类通用模板；同步补充 `DECK TOP` 回填语义、上下文数组移除步骤，并刷新 `cards_effects/cards_semantic` 产物，使 `UA31BT_MMM_1_035`、`040`、`042`、`043`、`048`、`055`、`065` 转为支持，`UA31BT_MMM_1_056` 保持整体未支持但已接入前半段公共模板。
- 影响文件或模块：`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`core/zone_manager.gd`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/card_design.md`、`docs/plan/development_plan.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：使用 `C:\\Users\\lenovo\\AppData\\Local\\Programs\\Python\\Python313\\python.exe tools\\compile_cards_effects.py` 成功刷新产物，输出 `Compiled 99 cards -> cards_effects.json`、`Supported abilities: 111`、`Unsupported abilities: 18`；随后静态复核 `data/cards/cards_semantic.json`，确认 `035/040/042/043/048/055/065` 已转为 `SUPPORTED`，`056 ON_ENTER` 仍为 `UNSUPPORTED`；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd` 时当前脚本长时间未结束，未拿到完整结果；执行 `res://docs/milestone_smoke_test.gd` 时暴露既有 `starter_a.txt/starter_b.txt` 读取失败并导致多项旧用例联动失败，本轮未将其作为新增模板回归通过依据。

- 日期：2026-03-30
- 变更类型：bugfix
- 变更摘要：定位并修复 `docs/cards_raw_minimal_duel_smoke_test.gd` 的“卡住”问题，确认脚本并非死循环，而是被 4 个错误断言阻塞；其中修正了 `043` 用例的预览命中特征数据，并把 `065` 用例拆成“非法同名选择回队列”和“合法不同名选择继续结算”两段验证，同时保留生命触发样例使用生命区顶牌的约束。
- 影响文件或模块：`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：执行 `D:\\CodexWork\\TcgDemo\\Godot\\Godot_v4.6.1-stable_win64_console.exe --headless --path D:\\CodexWork\\TcgDemo --script res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `通过: 48 / 失败: 0 / CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`；运行结束仍有既有 Godot `ObjectDB instances leaked at exit` 与 `resources still in use at exit` 提示，但不影响本轮脚本完成与断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：校正 `docs/plan/card_design.md`、`docs/plan/development_plan.md` 与 `docs/plan/mile_stone.md` 的计划口径，使其重新以 `cards_effects.json` 的 `abilities[*].status` 统计结果为准；同时复核 `UA31BT_MMM_1_056`，确认其并非陈旧说明，而是当前真实 DSL/IR 缺口，应继续保留为 `UNSUPPORTED` 并作为后续优先收口对象。
- 影响文件或模块：`docs/plan/card_design.md`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：使用显式 UTF-8 与脚本静态统计 `data/cards/cards_effects.json`，确认当前共 `99` 张卡、`111` 个 `SUPPORTED`、`18` 个 `UNSUPPORTED`，未支持能力分布在 `14` 张卡上，时机分布为 `MAIN_ACTIVATE 5 / ON_PLAY 5 / ON_ENTER 5 / ON_ATTACK 2 / ON_LEAVE 1`；随后复核 `tools/compile_cards_effects.py` 中 `UA31BT_MMM_1_056` 的专门分支及对应产物，确认“后半段临时能力授予仍未支持”仍为真实结论而非陈旧元数据；执行 `python tools/compile_cards_effects.py` 后统计保持 `111 / 18` 不变；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd` 结果为 `48` 项通过、`0` 项失败，执行 `res://docs/milestone_smoke_test.gd` 结果为 `30` 项通过、`0` 项失败。两次 Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：按 `docs/plan/card_design.md` 的当前 P0/P1 顺序，补齐 `UA31BT_MMM_1_056` 的真实 DSL/IR 缺口：新增“己方场上其他指定特征卡按卡张数计数”的 requirement，并把后半段能力统一接入为满足条件时对 `SOURCE_CARD` 赋予本回合临时 `IMPACT`；同时补充两条 `056` raw 样例回归，以及一条规则级 `TEMP_KEYWORD` 结束回合清理断言，最后同步刷新计划文档统计口径。
- 影响文件或模块：`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/milestone_smoke_test.gd`、`docs/plan/card_design.md`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：执行 `python tools/compile_cards_effects.py` 后，统计更新为 `112` 个 `SUPPORTED`、`17` 个 `UNSUPPORTED`，存在未支持能力的卡牌降为 `13` 张，时机分布更新为 `MAIN_ACTIVATE 5 / ON_PLAY 5 / ON_ENTER 4 / ON_ATTACK 2 / ON_LEAVE 1`；随后执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `50` 项通过、`0` 项失败，新增通过 `Raw Preview Summon 056 Without Bonus` 与 `Raw Preview Summon 056 With Bonus`；执行 `res://docs/milestone_smoke_test.gd`，结果为 `31` 项通过、`0` 项失败，新增通过“本回合临时关键词会在结束时清理且不残留运行时脏状态”。两次 Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：继续按 `card_design.md` 第一轮收口剩余未支持模板，新增“指定名称存在于指定区域”“来源 BP 达标”“仅限登场回合”“前线/能量线复合筛选”“按场上特征数量动态抬升阈值”等 requirement / provider，并补齐 `DRAW -> DISCARD`、场外回手、临时 `IMPACT / IMPACT_PLUS_1 / DOUBLE_ATTACK`、命中条件追加抽牌等通用 step 组合；同步把 `035`、`041`、`049`、`052`、`055`、`060`、`062`、`064`、`066`、`067` 的 raw 回归补进正式样例，并新增 `entered_this_turn` 跨回合清理断言。
- 影响文件或模块：`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`core/game_manager.gd`、`data/card_instance.gd`、`data/cards/cards_effects.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/milestone_smoke_test.gd`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：执行 `python tools/compile_cards_effects.py` 成功刷新产物，输出 `Compiled 99 cards -> cards_effects.json`、`Supported abilities: 123`、`Unsupported abilities: 6`；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `60` 项通过、`0` 项失败；执行 `res://docs/milestone_smoke_test.gd`，结果为 `32` 项通过、`0` 项失败。两次 Godot 退出时仍出现既有 `ObjectDB instances leaked at exit` 与 `resources still in use at exit` 告警，但未影响本轮断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：继续按优先级清零剩余尾项，补齐 `035` 位置交换、`036` 可选返手后动态减 BP、`041` 退场时取回 raid 元卡、`044` raid 内全体增益与公开命中后加手/未命中上或下置、`067` 按本次指定目标减 AP 等最后几类原子模板；同时扩展了解释器对 `OPTION_SET`、按来源玩家归属移动、按参考名判断全场名称集合、按 provider 动态 BP 变化、以及 raid 叠放元卡取回的支持，并把对应 raw 回归正式补入样例。
- 影响文件或模块：`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`core/game_manager.gd`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：执行 `python tools/compile_cards_effects.py` 后，统计更新为 `Compiled 99 cards -> cards_effects.json`、`Supported abilities: 128`、`Unsupported abilities: 0`；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `66` 项通过、`0` 项失败，新增通过 `Raw Main Activate Swap 035`、`Raw Optional Return Debuff 036`、`Raw On Leave Returns Raid Base 041`、`Raw Raid Team Buff 044`、`Raw Raid Preview Reward 044`、`Raw Event Targeted Cost Discount 067`；执行 `res://docs/milestone_smoke_test.gd`，结果为 `32` 项通过、`0` 项失败。Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响本轮断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：同步更新 `docs/plan/card_design.md` 的计划口径，将文档从“仍有 17 个未支持能力”的旧状态刷新为“128 个已支持、0 个未支持”的当前真实基线，并把主线从 Phase 1 补模板切换为 Phase 2 共性稳定性回归与 P2 非高风险收尾项，同时保留现有 IR 冻结约束与验证入口说明。
- 影响文件或模块：`docs/plan/card_design.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：使用显式 UTF-8 复读并更新 `docs/plan/card_design.md`；以 `python tools/compile_cards_effects.py` 输出 `Supported abilities: 128`、`Unsupported abilities: 0` 为文档基线，并对照最近一次 `res://docs/cards_raw_minimal_duel_smoke_test.gd` 的 `66 / 0` 与 `res://docs/milestone_smoke_test.gd` 的 `32 / 0` 结果，确认计划文档已与当前仓库真实状态一致。本轮仅同步计划文档与日志，未新增代码实现。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：按“接口和断言先锁，再做 2 路并行”的方式落地 P1 共性稳定性回归：核心运行时主链先完成 residue 风险复核，确认暂不需要修改 `core/`；随后新增 `docs/runtime_residue_smoke_test.gd`，专项覆盖显式目标选择恢复、延迟效果结算/过期、战斗后离场与叠放离场、生命触发混合显式决策链四类运行时脏状态回归。
- 影响文件或模块：`docs/runtime_residue_smoke_test.gd`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：先执行 `res://docs/runtime_residue_smoke_test.gd` 的沙箱内尝试，因 Godot 既有 `user://logs` 写入失败触发崩溃，按仓库约定改为沙箱外重跑；沙箱外执行结果为 `4` 项通过、`0` 项失败。随后复跑 `res://docs/milestone_smoke_test.gd`，结果为 `32` 项通过、`0` 项失败；复跑 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `66` 项通过、`0` 项失败。三次运行结束时仍有既有 `ObjectDB instances leaked at exit` 与 `resources still in use at exit` 告警，但未影响断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：新增 `docs/runtime_residue_smoke_test.gd` 作为规则共性稳定性专项回归入口，集中覆盖显式目标选择恢复、延迟效果过期、战斗后与叠放离场的 `battle_context` 清理，以及生命触发与显式目标选择混合链的运行时收尾；本轮仅修改 `docs/` 下文件，没有触碰 `core/` 或 `data/`。
- 影响文件或模块：`docs/runtime_residue_smoke_test.gd`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：执行 `D:\CodexWork\TcgDemo\Godot\Godot_v4.6.1-stable_win64_console.exe --headless --path D:\CodexWork\TcgDemo --script res://docs/runtime_residue_smoke_test.gd`，结果为 `通过: 4 / 失败: 0`；随后执行 `D:\CodexWork\TcgDemo\Godot\Godot_v4.6.1-stable_win64_console.exe --headless --path D:\CodexWork\TcgDemo --script res://docs/milestone_smoke_test.gd`，结果为 `通过: 32 / 失败: 0`。两次退出仍有既有 `ObjectDB instances leaked at exit` 与 `resources still in use at exit` 提示，但不影响本轮断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：同步更新 `docs/plan/card_design.md` 的 P1 进展口径，明确“规则共性稳定性回归”已从单一主冒烟入口扩展为“`milestone_smoke_test + runtime_residue_smoke_test`”双入口，并把已落地的 4 类专项脏状态回归和最新验证基线写回计划文档。
- 影响文件或模块：`docs/plan/card_design.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：使用显式 UTF-8 复读并更新 `docs/plan/card_design.md`；对照本轮已完成的专项回归结果，确认当前验证基线为 `docs/runtime_residue_smoke_test.gd` `4 / 0`、`docs/milestone_smoke_test.gd` `32 / 0`、`docs/cards_raw_minimal_duel_smoke_test.gd` `66 / 0`，且文档中的 P1 入口、重点观察项与后续顺序已与仓库当前状态一致。本轮仅同步计划文档与日志，未新增运行时代码改动。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：新增 `docs/plan/ui_art_style_guide.md` 作为正式 UI 美术规范文档，完整定义暗黑竞技风数字卡桌的色彩系统、战场背景、七个关键区域、卡槽、卡牌、图标、按钮、面板、标签、Tooltip、字体、间距、圆角、阴影、动效与资源命名基线；同时在 `docs/plan/development_plan.md` 中补充该文档作为后续 UI 视觉出稿与素材整理的统一依据。
- 影响文件或模块：`docs/plan/ui_art_style_guide.md`、`docs/plan/development_plan.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：静态复查新增文档结构，确认按顺序包含用户要求的 14 个章节，并对 `FRONT LINE`、`ENERGY LINE`、`AP AREA`、`LIFE`、`DECK`、`OUTSIDE`、`REMOVED` 七个区域分别给出区域层级、视觉重点、高亮策略、边框/底色/标题样式与槽位结构说明；全文补齐颜色 Hex、尺寸、圆角、描边、间距、阴影与动效建议，且未包含规则说明、实现代码、伪代码、节点树或数据结构。本轮为文档交付，不涉及运行时代码，因此未执行 Godot 冒烟测试。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：根据用户提供的战场背景图，收紧 `docs/plan/ui_art_style_guide.md` 的视觉口径，使规范从“偏数字面板化的暗黑竞技风”调整为“红色竞技桌垫感 + 轻量数字界面感”的混合方向；重点修正了背景层次、桌垫红色主基调、几何线装饰控制、区域标题承托样式、槽位白色轮廓线、圆角尺度与整体“印刷分区转屏幕 UI”的表达方式。
- 影响文件或模块：`docs/plan/ui_art_style_guide.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：静态复读更新后的 `docs/plan/ui_art_style_guide.md`，确认文档仍保持 14 个章节结构不变，并在不引入规则说明、实现方案、代码或节点树的前提下，将背景方案、七个区域、卡槽样式和最终视觉原则统一调整为更贴近参考战场底图的版本；同时复查 `FRONT LINE`、`ENERGY LINE`、`AP AREA`、`LIFE`、`DECK`、`OUTSIDE`、`REMOVED` 七个区域仍保留独立层级、高亮与样式定义。本轮为文档更新，不涉及运行时代码，因此未执行 Godot 冒烟测试。
