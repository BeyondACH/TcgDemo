# 2026-03-30

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：根据 `data/cards/cards_effects.json` 与 `data/cards/cards_semantic.json` 的当前实际产物，重写 `docs/plan/card_design.md`，将其从“76 个已支持、0 个未支持”的过期状态更新为“99 张卡、104 个已支持能力、25 个未支持能力、分布于 19 张卡”的待办清单，并按可复用的 requirement / target / step / play rule 缺口重新归类，补充卡牌到能力缺口映射与推荐实施顺序。
- 影响文件或模块：`docs/plan/card_design.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：使用显式 UTF-8 读取 `docs/rules/rule.md`、`docs/plan/development_plan.md`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json` 与最近日志；静态统计确认 `cards_effects.json` 当前为 `99` 张卡、`104` 个已支持能力、`25` 个未支持能力、`19` 张卡存在未支持项，且未支持时机分布为 `ON_ENTER 9 / ON_PLAY 6 / MAIN_ACTIVATE 5 / ON_ATTACK 2 / ON_LIFE_TRIGGER 2 / ON_LEAVE 1`；随后复查 `docs/plan/card_design.md`，确认文档内容已与当前编译产物一致。本轮仅更新计划文档与日志，未改动运行时代码，因此未执行 Godot 冒烟测试。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：落实第一批未支持原子能力模板，新增 `OUTSIDE -> FRONT_LINE` 过滤登场、`PREVIEW_TOP_DECK` 检索/重排/余牌去向、以及 `HAND / 预览上下文 -> FRONT_LINE` 过滤登场三类通用模板；同步补充 `DECK TOP` 回填语义、上下文数组移除步骤，并刷新 `cards_effects/cards_semantic` 产物，使 `UA31BT_MMM_1_035`、`040`、`042`、`043`、`048`、`055`、`065` 转为支持，`UA31BT_MMM_1_056` 保持整体未支持但已接入前半段公共模板。
- 影响文件或模块：`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`core/zone_manager.gd`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/plan/card_design.md`、`docs/plan/development_plan.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：使用 `C:\\Users\\lenovo\\AppData\\Local\\Programs\\Python\\Python313\\python.exe tools\\compile_cards_effects.py` 成功刷新产物，输出 `Compiled 99 cards -> cards_effects.json`、`Supported abilities: 111`、`Unsupported abilities: 18`；随后静态复核 `data/cards/cards_semantic.json`，确认 `035/040/042/043/048/055/065` 已转为 `SUPPORTED`，`056 ON_ENTER` 仍为 `UNSUPPORTED`；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd` 时当前脚本长时间未结束，未拿到完整结果；执行 `res://docs/milestone_smoke_test.gd` 时暴露既有 `starter_a.txt/starter_b.txt` 读取失败并导致多项旧用例联动失败，本轮未将其作为新增模板回归通过依据。

- 日期：2026-03-30
- 变更类型：bugfix
- 变更摘要：定位并修复 `docs/cards_raw_minimal_duel_smoke_test.gd` 的“卡住”问题，确认脚本并非死循环，而是被 4 个错误断言阻塞；其中修正了 `043` 用例的预览命中特征数据，并把 `065` 用例拆成“非法同名选择回队列”和“合法不同名选择继续结算”两段验证，同时保留生命触发样例使用生命区顶牌的约束。
- 影响文件或模块：`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：执行 `D:\\CodexWork\\TcgDemo\\Godot\\Godot_v4.6.1-stable_win64_console.exe --headless --path D:\\CodexWork\\TcgDemo --script res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `通过: 48 / 失败: 0 / CARDS_RAW_MINIMAL_DUEL_SMOKE_OK`；运行结束仍有既有 Godot `ObjectDB instances leaked at exit` 与 `resources still in use at exit` 提示，但不影响本轮脚本完成与断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：校正 `docs/plan/card_design.md`、`docs/plan/development_plan.md` 与 `docs/plan/mile_stone.md` 的计划口径，使其重新以 `cards_effects.json` 的 `abilities[*].status` 统计结果为准；同时复核 `UA31BT_MMM_1_056`，确认其并非陈旧说明，而是当前真实 DSL/IR 缺口，应继续保留为 `UNSUPPORTED` 并作为后续优先收口对象。
- 影响文件或模块：`docs/plan/card_design.md`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：使用显式 UTF-8 与脚本静态统计 `data/cards/cards_effects.json`，确认当前共 `99` 张卡、`111` 个 `SUPPORTED`、`18` 个 `UNSUPPORTED`，未支持能力分布在 `14` 张卡上，时机分布为 `MAIN_ACTIVATE 5 / ON_PLAY 5 / ON_ENTER 5 / ON_ATTACK 2 / ON_LEAVE 1`；随后复核 `tools/compile_cards_effects.py` 中 `UA31BT_MMM_1_056` 的专门分支及对应产物，确认“后半段临时能力授予仍未支持”仍为真实结论而非陈旧元数据；执行 `python tools/compile_cards_effects.py` 后统计保持 `111 / 18` 不变；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd` 结果为 `48` 项通过、`0` 项失败，执行 `res://docs/milestone_smoke_test.gd` 结果为 `30` 项通过、`0` 项失败。两次 Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。

- 日期：2026-03-30
- 变更类型：功能更新
- 变更摘要：按 `docs/plan/card_design.md` 的当前 P0/P1 顺序，补齐 `UA31BT_MMM_1_056` 的真实 DSL/IR 缺口：新增“己方场上其他指定特征卡按卡张数计数”的 requirement，并把后半段能力统一接入为满足条件时对 `SOURCE_CARD` 赋予本回合临时 `IMPACT`；同时补充两条 `056` raw 样例回归，以及一条规则级 `TEMP_KEYWORD` 结束回合清理断言，最后同步刷新计划文档统计口径。
- 影响文件或模块：`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/milestone_smoke_test.gd`、`docs/plan/card_design.md`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-03-30.md`
- 验证方式与结果：执行 `python tools/compile_cards_effects.py` 后，统计更新为 `112` 个 `SUPPORTED`、`17` 个 `UNSUPPORTED`，存在未支持能力的卡牌降为 `13` 张，时机分布更新为 `MAIN_ACTIVATE 5 / ON_PLAY 5 / ON_ENTER 4 / ON_ATTACK 2 / ON_LEAVE 1`；随后执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `50` 项通过、`0` 项失败，新增通过 `Raw Preview Summon 056 Without Bonus` 与 `Raw Preview Summon 056 With Bonus`；执行 `res://docs/milestone_smoke_test.gd`，结果为 `31` 项通过、`0` 项失败，新增通过“本回合临时关键词会在结束时清理且不残留运行时脏状态”。两次 Godot 退出时仍有既有 `ObjectDB` / resource 泄漏告警，但未影响断言通过。
