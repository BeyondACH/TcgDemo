# 2026-04-01

## 日志条目 1

- 日期：2026-04-01
- 变更类型：bugfix
- 变更摘要：修复 `tools/import_cards_raw_from_pic.ps1` 将沙箱内网络失败统一误报为 `official_page_not_found` 的问题；当前脚本已区分 `network_error`、`request_failed`、`detail_structure_missing`、`card_number_mismatch` 与真实 `official_page_not_found`，并同步明确卡图补录脚本默认应在沙箱外执行。本轮已在沙箱外成功补录 `UA31BT/MMM-1-001` 到 `034`，并重新生成下游 `cards_effects.json` 与 `cards_semantic.json`。
- 影响文件或模块：`tools/import_cards_raw_from_pic.ps1`、`data/cards/parser.md`、`data/cards/cards_raw.json`、`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`README.md`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-01.md`
- 验证方式与结果：在沙箱内执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\import_cards_raw_from_pic.ps1`，`UA31BT-MMM-1-001` 到 `034` 不再误报 `official_page_not_found`，而是统一输出 `network_error [..ConnectFailure]`；在沙箱外复跑同一脚本，结果为 `scanned=117`、`skipped_existing=83`、`added=34`、`failed=0`；随后执行 `python tools/compile_cards_effects.py`，结果为 `Compiled 132 cards -> cards_effects.json`、`Supported abilities: 158`、`Unsupported abilities: 25`。额外对 `UA31BT/MMM-1-001` 与 `UA31BT/MMM-1-035` 直接执行 `Invoke-WebRequest` 均返回 `200` 且包含 `cardNumData`，其中 `UA31BT/MMM-1-001` 已确认可被现有正则解析为正确卡号与名称，证明此前主因是环境网络失败而非官网缺卡。

## 日志条目 2

- 日期：2026-04-01
- 变更类型：功能更新
- 变更摘要：补充卡图补录文档流程，明确 `tools/generate_micro_card_images.ps1` 作为导入成功后的顺带步骤执行，用于同步补齐 `pic/micro/` 缺失缩略图。
- 影响文件或模块：`data/cards/parser.md`、`README.md`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-01.md`
- 验证方式与结果：核对 `tools/generate_micro_card_images.ps1` 的实际行为，确认脚本读取 `pic/` 顶层 `.png` 卡图、仅为 `pic/micro/` 缺失文件生成 84px 高 PNG 缩略图，文档、README、计划与里程碑中的操作顺序已统一为“先导入、再生成缩略图、最后编译下游数据”。

## 日志条目 3

- 日期：2026-04-01
- 变更类型：功能更新
- 变更摘要：实际执行 `tools/generate_micro_card_images.ps1`，为本轮新补录卡图同步生成 `pic/micro/` 缺失缩略图。
- 影响文件或模块：`pic/micro/`、`docs/logs/log_2026-04-01.md`
- 验证方式与结果：在沙箱外执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\generate_micro_card_images.ps1`，结果为 `scanned=117`、`skipped_existing=83`、`generated=34`，输出目录为 `D:\CodexWork\TcgDemo\pic\micro`；沙箱内同命令曾因 `pic/micro/` 写入被拒绝而触发 `GDI+` 通用保存错误，本次已通过沙箱外执行完成补齐。
