## 2026-04-11
- 变更类型：数据导入 / 工具补充
- 变更摘要：因当前环境缺少 PowerShell 运行时，新增 Python 版卡图导入脚本，并按 `data/cards/parser.md` 口径导入 `pic/MCR` 卡图到 `data/cards/MCR/cards_raw.json`。
- 影响文件：`tools/import_cards_raw_from_pic.py`、`data/cards/MCR/cards_raw.json`
- 验证方式：运行 `python tools/import_cards_raw_from_pic.py --series MCR` 并确认输出统计；运行 `python -m json.tool data/cards/MCR/cards_raw.json` 校验 JSON。
- 结果：成功生成 121 条 MCR 卡牌原始数据，JSON 结构有效。
- 后续事项：如需与 PowerShell 脚本完全对齐，可后续补齐失败原因分类与 `-RefreshExisting` 等参数行为。

## 2026-04-11（下游同步）
- 变更类型：下游同步 / 编译链路修复
- 变更摘要：修复 `tools/compile_cards_effects.py` 中遗留的冲突标记，并将 MCR 导入数据中的 `special_play_rule` 统一为对象结构后，完成 MCR 的 `cards_effects.json` 与 `cards_semantic.json` 生成。
- 影响文件：`tools/compile_cards_effects.py`、`tools/import_cards_raw_from_pic.py`、`data/cards/MCR/cards_raw.json`、`data/cards/MCR/cards_effects.json`、`data/cards/MCR/cards_semantic.json`
- 验证方式：运行 `python tools/compile_cards_effects.py`；执行三个 MCR JSON 的 `python -m json.tool` 校验；执行 `python -m unittest tests.test_compile_cards_effects.CompileCardsEffectsTests.test_compile_cards_effects_script_runs_from_workspace_root`。
- 结果：下游文件同步完成，编译脚本可正常执行，目标用例通过。
- 后续事项：后续新增系列可直接复用该链路做 raw->effects->semantic 同步。
