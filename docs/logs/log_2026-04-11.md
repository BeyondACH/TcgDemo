## 2026-04-11
- 变更类型：数据导入 / 工具补充
- 变更摘要：因当前环境缺少 PowerShell 运行时，新增 Python 版卡图导入脚本，并按 `data/cards/parser.md` 口径导入 `pic/MCR` 卡图到 `data/cards/MCR/cards_raw.json`。
- 影响文件：`tools/import_cards_raw_from_pic.py`、`data/cards/MCR/cards_raw.json`
- 验证方式：运行 `python tools/import_cards_raw_from_pic.py --series MCR` 并确认输出统计；运行 `python -m json.tool data/cards/MCR/cards_raw.json` 校验 JSON。
- 结果：成功生成 121 条 MCR 卡牌原始数据，JSON 结构有效。
- 后续事项：如需与 PowerShell 脚本完全对齐，可后续补齐失败原因分类与 `-RefreshExisting` 等参数行为。

## 2026-04-11（PR #17 审查问题修复）
- 变更类型：bugfix / 导入契约修正
- 变更摘要：修复 Python 导入器中 `cost_energy` 被错误解析为整数的问题，改为与能量图示一致的颜色映射字典；修复 `RAID` 卡 `special_play_rule` 输出为字符串的问题，改为结构化字典并补齐目标名与规则字段。
- 影响文件：`tools/import_cards_raw_from_pic.py`
- 验证方式：运行内联 Python 检查 `parse_card_page`，确认 `cost_energy` 为字典、`special_play_rule` 为包含 `type/raid_target_name/allow_from_hand/require_full_energy` 的字典。
- 结果：导入结果字段类型与下游 `CardDef`/规则消费契约一致。
- 后续事项：如后续发现官网 `needEnergyData` 或 RAID 文案出现新样式，再扩展解析模式并补样例回归。
