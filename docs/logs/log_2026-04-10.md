## 2026-04-10
- 变更类型：功能开发 / 测试回归
- 变更摘要：在 `tools/compile_cards_effects.py` 新增多组模板规则，覆盖“条件抽牌、BP 增减、可选支付 AP 造成伤害、手牌登场、看牌堆顶后上下放置、以下から1つ選ぶ分支选择”等文本族；统一通过模板 builder 产出 `requirements`、`target_specs`、`steps`、`limits`、`template_metadata` 并返回 `SUPPORTED`。
- 影响文件：`tools/compile_cards_effects.py`、`tests/test_compile_cards_effects.py`、`data/cards/TLR/cards_effects.json`
- 验证方式：`python -m unittest tests/test_compile_cards_effects.py`；`python tools/compile_cards_effects.py`；针对 `UA45BT_TLR_1_005/007/010/014/050/055/081` 检查能力状态与步骤类型。
- 结果：新增模板样例测试通过，相关目标文本条目已从 `UNSUPPORTED` 迁移为 `SUPPORTED`。
- 后续事项：继续补齐“分支选项具体子效果”模板化编译，逐步将 `・` 开头的分支文本接入同一族规则，降低事件分支后续条目的 `UNSUPPORTED` 数量。

- 变更类型：规则引擎能力扩展
- 变更摘要：在 `RequirementMatcher` 新增目标集合聚合、动态阈值、互斥去重 requirement（`TARGET_SET_BP_SUM_LTE`、`TARGET_SET_DYNAMIC_SUM_LTE`、`TARGET_SET_UNIQUE`、`TARGET_SET_DISJOINT`）；在 `StepExecutor` 新增组合约束选目标、显式分支、条件串联步骤 handler（`SELECT_TARGETS_BY_COMBINATION`、`SET_CHOICE_MODE`、`EXECUTE_CHOICE_BRANCH`、`RUN_COMPOSITE_IF`），并在 `_handlers` 注册稳定分发，避免落入黑盒 fallback。
- 影响文件：`core/effects/requirement_matcher.gd`、`core/effects/step_executor.gd`、`docs/rules/rule.md`
- 验证方式：`python -c "from pathlib import Path; Path('docs/rules/rule.md').read_text(encoding='utf-8'); Path('docs/logs/log_2026-04-10.md').read_text(encoding='utf-8')"`；`python -m compileall core/effects`
- 结果：新增 requirement/step 类型与文档口径一致，中文文档 UTF-8 重读正常，脚本语法检查通过。
- 后续事项：后续可在卡牌编译链路补充 `selection_constraints` 自动映射，并补一组“最多2枚且总BP≤X + choice 分支”端到端样例。

## 2026-04-10
- 变更类型：功能扩展 / 规则编译模板补齐 / 验证补充
- 变更摘要：在编译链路新增 `BP_SUM_LIMIT_REMOVE` 与“牌堆预览+名称包含过滤+加手后弃牌”模板；`StepExecutor` 新增 `SELECT_TARGETS_WITH_SUM_LIMIT`（复用组合约束选择逻辑）；`RequirementMatcher` 新增 `NAME_CONTAINS` 过滤；补充最小对局样例测试入口。
- 影响文件：`tools/compile_cards_effects.py`、`core/effects/step_executor.gd`、`core/effects/requirement_matcher.gd`、`tests/test_compile_cards_effects.py`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/reports/unsupported_atomic_capabilities.md`
- 验证方式：`python -m unittest tests/test_compile_cards_effects.py`、`python tools/compile_cards_effects.py`、`./Godot/Godot_v4.6.1-stable_win64_console.exe --headless --path . --script res://docs/cards_raw_minimal_duel_smoke_test.gd`
- 结果：编译与单测通过；`SEMANTIC_OVERRIDE_REQUIRED` 从 3 降到 1；Godot 冒烟在当前环境缺少可执行文件未运行。
- 后续事项：补齐 `UA45BT_TLR_1_082` 的“送回牌库底”模板族并补对应最小对局验证。

## 2026-04-10
- 变更类型：文档整理
- 变更摘要：合并 `docs/plan/mile_stone.md` 与 `docs/plan/milestone.md`，统一为单一里程碑文档 `docs/plan/milestone.md`，并在文档中明确“现状盘点 + 后续路线”双分区，消除重复维护入口。
- 影响文件：`docs/plan/milestone.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-10.md`
- 验证方式：`python tools/check_utf8_docs.py`（若存在）；`git diff -- docs/plan/milestone.md docs/plan/mile_stone.md docs/logs/log_2026-04-10.md`
- 结果：里程碑文档已完成合并，重复文件已删除，中文内容按 UTF-8 正常读取。
- 后续事项：后续仅维护 `docs/plan/milestone.md`，避免再出现同主题多文件分叉。

## 2026-04-10
- 变更类型：计划文档对齐
- 变更摘要：对齐 `milestone` 与 `task_breakdown` 的职责边界：在里程碑文档中补充“里程碑口径 vs 执行拆解口径”说明；在任务拆解文档新增更新时间与“两周执行排期（2026-04-10 ~ 2026-04-24）”，明确 P0/P1 的周粒度 DoD。
- 影响文件：`docs/plan/milestone.md`、`docs/plan/task_breakdown.md`、`docs/logs/log_2026-04-10.md`
- 验证方式：`python tools/check_utf8_docs.py`；`python - <<'PY'\nfrom pathlib import Path\nfor p in [\n    'docs/plan/milestone.md',\n    'docs/plan/task_breakdown.md',\n    'docs/logs/log_2026-04-10.md',\n]:\n    Path(p).read_text(encoding='utf-8')\nprint('utf8-ok')\nPY`；`git diff -- docs/plan/milestone.md docs/plan/task_breakdown.md docs/logs/log_2026-04-10.md`
- 结果：三份中文文档已通过 UTF-8 重读校验，计划层级与执行层级职责清晰，可直接进入周粒度执行。
- 后续事项：若 Week 1 P0 三项提前完成，优先把 `P1-1` 分支子效果模板化中的“分支内再选目标”场景提前进入当周回归。

## 2026-04-10
- 变更类型：P0 回归落地 / 运行时一致性修复
- 变更摘要：落地 `task_breakdown` 的 Iteration A 三项核心回归：1）`TargetSelector` 恢复阶段新增数量上限、重复目标、总 BP 上限、动态阈值与互斥集合校验；2）`GameManager` 在 `ABILITY_TARGET_SELECTION` 非法输入时回插待决策并返回稳定 reason，避免错误输入误消费决策队列；3）`runtime_residue_smoke_test` 新增组合约束恢复、choice 分支、`RUN_COMPOSITE_IF` 的暂停/恢复与默认分支回归。
- 影响文件：`core/effects/target_selector.gd`、`core/game_manager.gd`、`docs/runtime_residue_smoke_test.gd`
- 验证方式：`python -m compileall core docs/runtime_residue_smoke_test.gd`；`./Godot/Godot_v4.6.1-stable_win64_console.exe --headless --path . --script res://docs/runtime_residue_smoke_test.gd`
- 结果：静态语法检查通过；当前环境缺少 Godot 可执行文件，已记录为待在可执行环境复跑的冒烟项。
- 后续事项：下一步按 `task_breakdown` Week 2 进入 `P1-1/P1-2`（分支子效果模板化 + `selection_constraints` 编译映射），并把本轮新增错误码接入 UI 映射文档。

## 2026-04-10
- 变更类型：P1 Week 2 开始 / 编译器模板扩展
- 变更摘要：进入 `task_breakdown` Week 2 的 `P1-1/P1-2`：1）分支模板从 `EXECUTE_BRANCH + PENDING_BRANCH_EFFECT` 升级为 `EXECUTE_CHOICE_BRANCH`，并支持分支内抽牌/伤害/加 BP 与已有模板的结构化子步骤编译；2）`selection_constraints` 编译映射统一落到 `SELECT_TARGETS_BY_COMBINATION`，新增“按自家能量线张数 × 系数”的 `max_sum_provider` 动态阈值模板；3）补充暂停/恢复错误码 UI 映射文档，固化 reason→文案建议。
- 影响文件：`tools/compile_cards_effects.py`、`tests/test_compile_cards_effects.py`、`data/cards/TLR/cards_effects.json`、`docs/plan/ui_reason_mapping.md`、`docs/logs/log_2026-04-10.md`
- 验证方式：`python -m unittest tests.test_compile_cards_effects`；`python tools/compile_cards_effects.py`；`python tools/check_utf8_docs.py`
- 结果：编译器测试通过；系列编译可完成且支持能力统计提升；中文文档 UTF-8 检查通过。
- 后续事项：继续按 Week 2 DoD 补“分支内目标选择 + requirements”样例，观察 `SUPPORTED/UNSUPPORTED` 变化并纳入下一轮日志。
