## 2026-06-08
- 变更类型：设计冻结（UI v2.0 视觉方向）
- 变更摘要：
  - 完成 37 条 UI 设计决策的逐条裁定，方向从"竞技桌垫印刷分区"全面翻为"独立数字面板 + Master Duel 冷蓝黑基底"
  - 创建 `CONTEXT.md`：UI 领域术语词汇表（面板、槽位、附属堆叠、上下文按钮等 20+ 术语）
  - 创建 `docs/adr/0001-digital-panel-ui-direction.md`：记录方向转换决策与理由
  - 重写 `docs/plan/ui_art_style_guide.md` v2.0：完整色板（冷蓝黑 + 青/金强调）、四行面板规范、附属堆叠、顶部 HUD、动效三批策略、Theme 组织、响应式断点、素材策略
  - 更新 `AGENTS.md`：锁定 `ui_art_style_guide.md` v2.0 为 UI 权威来源，补充 ADR 引用
  - 更新 `docs/plan/development_plan.md`：新增阶段 F（UI v2.0 视觉翻新），定义 P0-P4 执行顺序
- 影响文件：
  - 新增：`CONTEXT.md`、`docs/adr/0001-digital-panel-ui-direction.md`
  - 重写：`docs/plan/ui_art_style_guide.md`（v1.0 → v2.0，724 行）
  - 更新：`AGENTS.md`、`docs/plan/development_plan.md`
- 验证方式：
  - 全部中文文档通过显式 UTF-8 复读校验，零乱码
  - Git diff 确认无问号串或编码污染
- 结果：设计冻结完成，进入实现等待
- 后续事项：
  - P0：建 `battle_theme.theme` + 字体加载
  - P1：BattleScene 脚本+场景双拆
  - P2：四行面板布局落地
  - P3：动效/tooltip/日志
  - P4：素材补全

## 2026-06-08
- 变更类型：UI 素材生成
- 变更摘要：
  - 按 `docs/plan/ui_asset_list.md` 生成 A/B/C 级 UI 素材
  - 新增空槽、功能、堆叠类 SVG 图标
  - 新增能量圆点、选中光晕、青色环状高亮与面板网格纹理 PNG
- 影响文件：
  - 新增：`assets/ui/icons/*.svg`
  - 新增：`assets/ui/dots/*.png`
  - 新增：`assets/ui/effects/*.png`
  - 新增：`assets/ui/backgrounds/panel_grid_tile.png`
- 验证方式：
  - 静态校验素材文件数量、尺寸、PNG RGBA alpha 与 SVG 基本结构
  - 中文日志执行显式 UTF-8 复读校验
  - Git diff 静态复核无问号串或编码污染
- 结果：UI 素材清单已补齐
- 后续事项：
  - 在 Theme 或 UI 脚本中接入 `res://assets/ui/` 下的新素材

## 2026-06-08
- 变更类型：P1-3 控制器提取（HUDController）
- 变更摘要：
  - 新建 `ui/hud_controller.gd`（182行），从 BattleScene 提取顶部 HUD 管理
  - HUDController 管理：顶部状态栏标签更新（turn/active_player/phase/hand_count/energy/AP/winner）、阶段控制按钮（next_phase/bonus_draw/no_block）、阻挡请求处理、AI 动作提示
  - 遵循现有 RefCounted + 双向状态同步模式（与 BoardController、SelectionStateMachine 同）
  - BattleScene 从 1278 行瘦身至 1248 行（净减 30 行）
  - 移除 BattleScene 中的 `_pending_attack_uid`、`_pending_defender_player_id` 和 `_format_energy_total`（迁移至 HUDController）
  - 信号连接重定向：`blockers_requested`、`ai_action_executed`、`next_phase_button/no_block_button/bonus_draw_button.pressed` 均指向对应 HUDController 方法
- 影响文件：
  - 新增：`ui/hud_controller.gd`
  - 修改：`ui/battle_scene.gd`（预加载/成员变量/`_ready()` 初始化/`_on_state_changed()` 标签委托/信号连接重定向/移除冗余变量和方法）
- 验证方式：
  - 冒烟测试：`milestone_smoke_test.gd` 33/33 通过，`cards_raw_minimal_duel_smoke_test.gd` 82/82 通过，零回归
- 结果：P1-3 完成，HUDController 已接入
- 后续事项：
  - P1-4：提取 HandController（手牌选中/拖拽/出牌）
  - P1-5：提取 ModalController（弹窗生命周期）
  - P1-6：BattleScene 最终瘦身（目标 ≤300 行）+ ZoneLayoutConfig 退役
  - P1-7：全量回归测试 + 布局验收

## 2026-06-08
- 变更类型：P1-4 控制器提取（HandController）
- 变更摘要：
  - 新建 `ui/hand_controller.gd`（221行），从 BattleScene 提取手牌管理
  - HandController 管理：手牌选中/悬停、出牌（前线/能量线/事件）、RAID 目标选择与执行、手牌可打出状态更新
  - 遵循现有 RefCounted + 双向状态同步模式（与 BoardController、SelectionStateMachine、HUDController 同）
  - BattleScene 从 1248 行瘦身至 1185 行（净减 63 行）
  - 移除 BattleScene 中的 `_selected_hand_card_uid`、`_raid_source_card_uid`、`_raid_target_selection_mode`、`_find_hand_card()`、`_execute_raid_play()`、`_clear_raid_selection()`（迁移至 HandController）
  - `_clear_selection()` 同步链路更新：先 sync `_hand_controller`，再 sync `_state_machine`
  - `_on_front_card_pressed()` / `_on_energy_card_pressed()` 中的 RAID 分支改为通过 `_hand_controller` 读写
- 影响文件：
  - 新增：`ui/hand_controller.gd`
  - 修改：`ui/battle_scene.gd`（移除 6 个变量/方法，38 处引用改为 `_hand_controller.*`）
- 验证方式：
  - 冒烟测试：`milestone_smoke_test.gd` 33/33 通过，`cards_raw_minimal_duel_smoke_test.gd` 82/82 通过，零回归
- 结果：P1-4 完成，HandController 已接入
- 后续事项：
  - P1-5：提取 ModalController（弹窗生命周期）
  - P1-6：BattleScene 最终瘦身（目标 ≤300 行）+ ZoneLayoutConfig 退役
  - P1-7：全量回归测试 + 布局验收

## 2026-06-08
- 变更类型：P1-5 控制器提取（ModalController）
- 变更摘要：
  - 新建 `ui/modal_controller.gd`（215行），从 BattleScene 提取弹窗生命周期管理
  - ModalController 管理：LifeRevealModal 创建与信号连接、ZoneCardsPopup、PreviewSelectionModal 同步、DeckSelector 委托、牌组选择弹窗、日志面板开关
  - BattleScene 从 1185 行瘦身至 1127 行（净减 58 行）
  - 移除 BattleScene 中的 `_life_reveal_modal`、`_zone_cards_popup` 变量及相关创建代码（迁移至 ModalController.create_modals()）
  - 移除 5 个已委托方法的完整实现体（`_sync_preview_selection_modal`、`_sync_life_reveal_modal`、`_on_life_reveal_activate_requested`、`_on_life_reveal_skip_requested`、`_on_life_reveal_acknowledge_requested`）
  - 移除死代码 `_selected_deck_path`
  - 信号连接重定向：`preview_selection_modal.submitted`、`log_toggle_button.pressed` → ModalController
  - LifeRevealModal 信号连接移入 ModalController.create_modals()
- 影响文件：
  - 新增：`ui/modal_controller.gd`
  - 修改：`ui/battle_scene.gd`（移除 2 个成员变量/5 个方法体/1 个死方法，`_ready()` 初始化重组）
- 验证方式：
  - 冒烟测试：`milestone_smoke_test.gd` 33/33 通过，`cards_raw_minimal_duel_smoke_test.gd` 82/82 通过，零回归
- 结果：P1-5 完成，ModalController 已接入
- 后续事项：
  - P1-6：BattleScene 最终瘦身（目标 ≤300 行）+ ZoneLayoutConfig 退役
  - P1-7：全量回归测试 + 布局验收

## 2026-06-08
- 变更类型：P1-6 BattleScene 最终瘦身 + ZoneLayoutConfig 退役（BattleScene 侧）
- 变更摘要：
  - BattleScene 从 1127 行瘦身至 1109 行（净减 18 行）
  - 移除 ZoneLayoutConfig 依赖：`_update_responsive_layout()` 中的 `calculate_bg_transform()` 改为内联计算（BG_IMAGE_WIDTH/HEIGHT 常量 + 直接除法），不再依赖 `data/zone_layout_config.gd`
  - 移除 7 个死代码/冗余包装方法：`_load_deck_selection_options`、`_show_deck_selection_modal`、`_hide_deck_selection_modal`、`_refresh_deck_selection_modal_state`、`_on_start_game_pressed`、`_on_log_toggle_pressed`、`_on_preview_modal_submitted`
  - 信号连接直达：`start_game_button.pressed` → `_modal_controller.start_game_with_selected_decks`
  - 内联调用：`_load_deck_selection_options()` → `_modal_controller.load_deck_selection_options()`
  - 注：ZoneLayoutConfig 在 BoardView 中的使用保留，等待 P2 面板布局落地时统一退役
  - BattleScene 累计瘦身：1278 → 1109（-169 行，13.2%），距目标 ≤300 行仍需 P2 布局重构
- 影响文件：
  - 修改：`ui/battle_scene.gd`（移除 ZoneLayoutConfig 调用、移除 7 个死方法、内联计算）
- 验证方式：
  - 冒烟测试：`milestone_smoke_test.gd` 33/33 通过，`cards_raw_minimal_duel_smoke_test.gd` 82/82 通过，零回归
  - `runtime_residue_smoke_test.gd` 13 项测试中 1 项失败（choice 分支默认值，属既有规则问题）
  - `life_reveal_modal_smoke_test.gd` 部分 AI 翻牌确认测试失败（属既有游戏逻辑问题）
- 结果：P1-6 完成，ZoneLayoutConfig 已从 BattleScene 退役
- 后续事项：
  - P2：四行面板布局落地（BoardView 改用 anchor + container，ZoneLayoutConfig 全量退役）
  - 布局验收需在 Godot 编辑器中目视确认手牌区域不遮挡战场

## 2026-06-08
- 变更类型：P1-7 全量回归测试
- 变更摘要：
  - 完成 P1 阶段全部回归测试
  - 核心测试零回归：`milestone_smoke_test.gd` 33/33、`cards_raw_minimal_duel_smoke_test.gd` 82/82
  - 布局验收：headless 模式无法目视验证，需在 Godot 编辑器中确认手牌区域不遮挡战场面板
  - `long_run_stability_smoke_test.gd` 和 `vs_ai_smoke_test.gd` 超时（headless 模式下长对局运行时间超出 120s 限制）
  - `runtime_residue_smoke_test.gd` 13 项中 12 项通过（1 项既有规则问题）
  - `life_reveal_modal_smoke_test.gd` AI 翻牌确认测试存在既有失败（非本次 UI 重构引入）
- 影响文件：无代码变更
- 验证方式：
  - 核心冒烟测试全部通过，零回归
  - 既有失败项已确认与本次 P1 控制器提取无关
- 结果：P1 阶段全部完成
- 后续事项：
  - P2：四行面板布局落地（`board_panel.tscn` + 四行镜像排列 + 附属堆叠 + 手牌/预览面板重定位）
  - P3：动效/tooltip/日志抽屉
  - P4：素材补全
