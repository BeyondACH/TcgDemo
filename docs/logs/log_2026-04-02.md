# 2026-04-02 日志

- 日期：2026-04-02
- 变更类型：功能更新
- 变更摘要：完成 `tools/compile_cards_effects.py` 识别层首轮注册表化，主入口改为 `registry -> legacy fallback`，并抽离首批模板族，保持编译结果不变。
- 影响文件或模块：`tools/compile_cards_effects.py`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`README.md`、`docs/logs/log_2026-04-02.md`
- 验证方式与结果：执行 `python -m py_compile tools/compile_cards_effects.py` 通过；执行 `python tools/compile_cards_effects.py` 输出 `Compiled 117 series cards across 1 directories.`、`Runtime card total with base samples: 132`、`Supported abilities: 177`、`Unsupported abilities: 6`，与重构前统计一致。

- 变更类型：功能更新
- 变更摘要：收口 `docs/plan/card_design.md` 剩余 P1 能力，补齐 `UA31BT/MMM-1-018`、`019`、`027`、`029`、`034` 的编译模板、运行时费用修正与结果分支，并将正式 MMM raw 基线更新为 `180 / 0`。
- 影响文件或模块：`tools/compile_cards_effects.py`、`core/effect_resolver.gd`、`core/effects/step_executor.gd`、`core/effects/requirement_matcher.gd`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/runtime_residue_smoke_test.gd`、`docs/plan/card_design.md`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`README.md`、`docs/logs/log_2026-04-02.md`
- 验证方式与结果：执行 `python tools/compile_cards_effects.py` 输出 `Supported abilities: 183`、`Unsupported abilities: 0`；静态统计 `data/cards/MMM/cards_effects.json` 确认正式 raw 口径为 `180 / 0`；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd` 结果为 `80 / 0`，执行 `res://docs/runtime_residue_smoke_test.gd` 结果为 `9 / 0`，执行 `res://docs/milestone_smoke_test.gd` 结果为 `33 / 0`。

- 变更类型：功能更新
- 变更摘要：为 AI 控制器补齐运行时动作节拍推进、`ai_action_executed` 信号和战斗界面顶部轻量提示位；保留 `drive_controllers()` 的同步语义，避免影响现有 headless smoke。
- 影响文件或模块：`core/controllers/controller_manager.gd`、`core/game_manager.gd`、`ui/battle_scene.gd`、`scenes/battle_scene.tscn`、`docs/vs_ai_smoke_test.gd`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`README.md`、`docs/logs/log_2026-04-02.md`
- 验证方式与结果：执行 `res://docs/vs_ai_smoke_test.gd` 结果为 `4 / 0`，新增断言确认同步 `drive_controllers()` 会发出 `ai_action_executed`；执行 `res://docs/milestone_smoke_test.gd` 结果为 `33 / 0`；执行 `--layout-probe` 结果为 `1920x1080 PASS`，确认顶部提示位未导致手牌区遮挡战场。

- 变更类型：Bugfix
- 变更摘要：修正生命翻牌弹窗中点击 `Activate` 后的 `RAID` 允许链路；当带 `LIFE_TRIGGER_RAID_CHOICE` 的生命触发卡当前不满足 AP / 能量 / 底座条件时，现会在发动后直接按规则兜底加入手牌，不再停留在仅剩禁用 `RAID_NOW` 的待决策态。
- 影响文件或模块：`core/effect_resolver.gd`、`core/life_trigger_manager.gd`、`core/effects/step_executor.gd`、`docs/cards_raw_minimal_duel_smoke_test.gd`、`docs/life_reveal_modal_smoke_test.gd`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`README.md`、`docs/logs/log_2026-04-02.md`
- 验证方式与结果：在沙箱外执行 `res://docs/life_reveal_modal_smoke_test.gd`，结果为 `7 / 0`，新增覆盖“非法 life-trigger RAID 仍可 Activate 且直接回手”；执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `80 / 0`，确认正式 raw 样例中的非法 RAID 生命触发会在 Activate 后直接加手；执行 `res://docs/milestone_smoke_test.gd`，结果为 `33 / 0`，确认生命伤害收尾、RAID 生命触发二选一与规则主链未被破坏。三次运行结束时仍有既有 `ObjectDB` / resource 泄漏警告，但未影响断言通过。


- 变更类型：文档更新
- 变更摘要：根据 2026-04-02 当日已落地实现与验证结果，刷新 `development_plan` 与 `mile_stone` 的当前基线和下一步计划；移除“剩余 6 条正式 raw 未支持能力”的旧口径，统一改为“正式 raw 已收口到 `180 / 0`，下一阶段主线为长链自动验证、生命触发交互稳定性与 AI 自动推进观察”。
- 影响文件或模块：`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-02.md`
- 验证方式与结果：使用显式 UTF-8 复读 `docs/plan/development_plan.md`、`docs/plan/mile_stone.md` 与 `docs/logs/log_2026-04-02.md`，确认中文内容无乱码；静态复核 `git diff`，确认验证基线已统一为 `cards_raw_minimal_duel_smoke_test.gd = 80 / 0`、`runtime_residue_smoke_test.gd = 9 / 0`、`life_reveal_modal_smoke_test.gd = 7 / 0`、`vs_ai_smoke_test.gd = 4 / 0`，且下一步方向已与当日日志一致。

- 变更类型：验证资产更新
- 变更摘要：新增 `docs/long_run_stability_smoke_test.gd`，把连续回合生命周期、延迟效果过期、离场触发链衔接、生命触发二选一收尾与 AI 自动推进完整对局稳定性收口到独立长链入口，并同步重排 `development_plan` / `mile_stone` 的验证职责口径。
- 影响文件或模块：`docs/long_run_stability_smoke_test.gd`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-02.md`
- 验证方式与结果：在沙箱外执行 `res://docs/long_run_stability_smoke_test.gd`，结果为 `5 / 0`；复跑 `res://docs/runtime_residue_smoke_test.gd` 结果为 `9 / 0`，`res://docs/vs_ai_smoke_test.gd` 结果为 `4 / 0`；执行 `python tools/check_utf8_docs.py` 输出 `UTF8_DOC_CHECK_OK 12 files`，确认本轮中文文档与新增验证入口口径一致。
