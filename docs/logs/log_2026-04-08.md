# 2026-04-08 日志

- 日期：2026-04-08
- 变更类型：Bugfix
- 变更摘要：修复 Phase E 执行前阻塞基线的编译回归，`core/effects/requirement_matcher.gd` 补回 `PlayerState` 预加载，恢复 `milestone` 主冒烟基线。
- 影响文件或模块：`core/effects/requirement_matcher.gd`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-08.md`
- 验证方式与结果：在沙箱外执行 `res://docs/milestone_smoke_test.gd`，结果为 `33 / 0`；确认 Phase E 工作树可以在该基线上继续推进。

- 变更类型：Bugfix
- 变更摘要：修正 AI 自动推进在生命翻牌 reveal 与多目标显式选择链上的待决策卡死问题；AI 现在会自动确认自身生命翻牌，且 `ABILITY_TARGET_SELECTION` 会按统一 `choices` 口径提交，不再遗留 `pending_life_reveal` 或目标选择队列残渣。
- 影响文件或模块：`core/controllers/controller_manager.gd`、`core/game_manager.gd`、`core/ai/simple_ai.gd`、`docs/vs_ai_smoke_test.gd`、`docs/runtime_residue_smoke_test.gd`、`docs/long_run_stability_smoke_test.gd`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-08.md`
- 验证方式与结果：在沙箱外执行 `res://docs/vs_ai_smoke_test.gd`，结果为 `5 / 0`；执行 `res://docs/runtime_residue_smoke_test.gd`，结果为 `10 / 0`；执行 `res://docs/long_run_stability_smoke_test.gd`，结果为 `6 / 0`。三者均确认 AI 长链不再遗留待决策或 residue 脏状态。

- 变更类型：验证资产更新
- 变更摘要：为 Phase E 补齐跨回合 delayed leave + AI pacing 长链断言，并把 runtime residue 与 VS AI 的基线同步抬升到最新口径，形成更完整的长链冻结基线。
- 影响文件或模块：`docs/long_run_stability_smoke_test.gd`、`docs/runtime_residue_smoke_test.gd`、`docs/vs_ai_smoke_test.gd`、`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-08.md`
- 验证方式与结果：在沙箱外执行 `res://docs/cards_raw_minimal_duel_smoke_test.gd`，结果为 `80 / 0`；复跑 `res://docs/milestone_smoke_test.gd`、`res://docs/runtime_residue_smoke_test.gd`、`res://docs/long_run_stability_smoke_test.gd` 与 `res://docs/vs_ai_smoke_test.gd`，结果分别为 `33 / 0`、`10 / 0`、`6 / 0`、`5 / 0`，确认当前 Phase E 冻结基线一致。

- 变更类型：文档更新
- 变更摘要：同步刷新 `development_plan` 与 `mile_stone` 的阶段口径，把 Phase E 状态从“下一阶段主线”更新为“进行中”，并统一写入 2026-04-08 的修复结果与验证基线。
- 影响文件或模块：`docs/plan/development_plan.md`、`docs/plan/mile_stone.md`、`docs/logs/log_2026-04-08.md`
- 验证方式与结果：使用显式 UTF-8 复读 `docs/plan/development_plan.md`、`docs/plan/mile_stone.md` 与 `docs/logs/log_2026-04-08.md`，并结合 `git diff` 静态复核，确认中文内容无新增乱码，且文档口径已统一为 `milestone = 33 / 0`、`cards_raw = 80 / 0`、`runtime_residue = 10 / 0`、`long_run = 6 / 0`、`vs_ai = 5 / 0`。
