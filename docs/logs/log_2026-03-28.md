# 2026-03-28

- 日期：2026-03-28
- 变更类型：功能更新
- 变更摘要：新增 `pic` 顶层图片到 `cards_raw.json` 的官网补录脚本，按图片文件名推导卡号并抓取 Union Arena 官方详情页数据；导入时会跳过 `cards_raw.json` 中已存在的卡牌编号，不覆盖旧条目。本次实际补录 32 张缺失卡，使 `cards_raw.json` 总数从 51 条提升到 83 条。
- 影响文件或模块：`tools/import_cards_raw_from_pic.ps1`、`data/cards/cards_raw.json`、`docs/logs/log_2026-03-28.md`
- 验证方式与结果：在沙箱外执行 `powershell -NoProfile -ExecutionPolicy Bypass -File D:\GodotWork\tcg-demo\tools\import_cards_raw_from_pic.ps1`，输出 `scanned=83`、`skipped_existing=51`、`added=32`、`failed=0`，确认仅补录缺失卡且未产生重复条目；随后用显式 UTF-8 方式复查 `data/cards/cards_raw.json`，确认文件总数为 83，新增样例 `UA31BT/MMM-1-035`、`UA31BT/MMM-1-067` 均包含官方详情页字段、`source_image` 与 `source_url` 一致。由于当前环境未安装可用 Python 解释器，未能执行 `tools/compile_cards_effects.py` 以同步刷新 `cards_effects.json` 与 `cards_semantic.json`，该阻塞已保留待后续环境补齐后处理。

- 日期：2026-03-28
- 变更类型：bugfix
- 变更摘要：修正官网图片导入脚本对发生产能图标的解析，补齐 `颜色重复字符`（如 `紫紫` 表示 2）与 `颜色加号`（如 `紫+` 表示 1）两种官方 `alt` 写法；随后使用 `-RefreshExisting` 重新抓取 `pic` 顶层卡，修复已导入卡牌的 `energy_provided` 空值问题。
- 影响文件或模块：`tools/import_cards_raw_from_pic.ps1`、`data/cards/cards_raw.json`、`docs/logs/log_2026-03-28.md`
- 验证方式与结果：先抓取 `UA31BT/MMM-1-035` 与 `UA31BT/MMM-1-046` 的官方详情页片段，确认 `generatedEnergyData` 的 `alt` 分别为 `紫紫` 与 `紫+`；随后在沙箱外执行 `powershell -NoProfile -ExecutionPolicy Bypass -File D:\GodotWork\tcg-demo\tools\import_cards_raw_from_pic.ps1 -RefreshExisting`，输出 `scanned=83`、`added=83`、`failed=0`。最后用显式 UTF-8 方式复查 `data/cards/cards_raw.json`，确认 `UA31BT_MMM_1_035` 的 `energy_provided` 已为 `{"PURPLE": 2}`，`UA31BT_MMM_1_046` 已为 `{"PURPLE": 1}`。当前环境仍未安装可用 Python 解释器，因此本轮仍未执行 `tools/compile_cards_effects.py`。

- 日期：2026-03-28
- 变更类型：功能更新
- 变更摘要：将本轮 `pic` 卡图补录到 `cards_raw.json` 的实际操作逻辑整理到 `data/cards/parser.md`，补充输入范围、文件名到卡号映射、官网抓取口径、去重与刷新规则、字段映射、发生产能 `alt` 特殊写法，以及导入后的检查项，便于后续直接按文档继续补卡。
- 影响文件或模块：`data/cards/parser.md`、`docs/logs/log_2026-03-28.md`
- 验证方式与结果：用显式 UTF-8 方式复查 `data/cards/parser.md`，确认文档已从方案稿改为操作说明，包含 `-RefreshExisting` 用法、`紫紫` / `紫+` 等 `energy_provided` 解析注意点，以及 `cards_raw.json` 更新后的检查与日志要求；本次为文档补充，未新增脚本执行。

- 日期：2026-03-28
- 变更类型：功能更新
- 变更摘要：在沙箱外使用本机 Python 3.11 执行下游编译，基于最新 `cards_raw.json` 刷新 `data/cards/cards_effects.json` 与 `data/cards/cards_semantic.json`，使新增的 `pic` 官网页面卡数据进入统一运行时产物。
- 影响文件或模块：`data/cards/cards_effects.json`、`data/cards/cards_semantic.json`、`docs/logs/log_2026-03-28.md`
- 验证方式与结果：在沙箱外执行 `C:\Users\ACH\AppData\Local\Programs\Python\Python311\python.exe tools\compile_cards_effects.py`，控制台输出 `Compiled 99 cards -> cards_effects.json`、`Supported abilities: 104`、`Unsupported abilities: 25`；随后静态检索确认 `data/cards/cards_effects.json` 与 `data/cards/cards_semantic.json` 中均已包含 `UA31BT_MMM_1_035`、`UA31BT_MMM_1_046`、`UA31ST_MMM_1_104` 等新增卡条目。PowerShell 直接解析超大 `cards_effects.json` 时仍有既有编码/解析噪声，因此本轮以编译输出和目标条目静态检索作为同步完成依据。

- 日期：2026-03-28
- 变更类型：功能更新
- 变更摘要：补充 `data/cards/parser.md` 的下游同步说明，明确允许在沙箱外调用 Python 执行 `tools/compile_cards_effects.py`，并给出优先使用明确解释器路径的推荐命令，方便后续直接按文档完成编译刷新。
- 影响文件或模块：`data/cards/parser.md`、`docs/logs/log_2026-03-28.md`
- 验证方式与结果：用显式 UTF-8 方式复查 `data/cards/parser.md`，确认“下游同步”章节已新增“允许在沙箱外调用 Python 执行编译”说明，并包含 `C:\Users\ACH\AppData\Local\Programs\Python\Python311\python.exe tools\compile_cards_effects.py` 与 `py tools\compile_cards_effects.py` 两种示例；本次为文档补充，未新增脚本执行。
