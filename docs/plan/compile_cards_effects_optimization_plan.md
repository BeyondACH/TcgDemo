# compile_cards_effects 后续编译优化实施方案

更新时间：2026-04-17

## 1. 目标与范围

### 1.1 目标

- 在不偏离 `docs/rules/rule.md` 的前提下，持续提升 `tools/compile_cards_effects.py` 的可维护性、可观测性与编译效率。
- 保持“按原子能力建模、禁止按卡硬编码、要求/步骤边界清晰”的既定路线。
- 基于当前正式系列 `cards_raw.json` 的高频文本分布，优先推进“模板家族参数化复用”，降低重复匹配规则和维护成本。

### 1.2 本轮覆盖范围

- 编译器识别层（模板注册、匹配策略、fallback 治理）
- 编译流程层（全量/增量、双遍构建优化）
- 验证与度量层（命中率、冲突检测、回归门禁）
- 文档与执行规范层（计划、日志、验收标准）

### 1.3 非目标

- 不改动 `rule.md` 规则语义。
- 不在运行时引入按卡分支解释逻辑。
- 不把 UI 层逻辑反向塞入规则核心。

---

## 2. 当前基线与问题画像

## 2.1 编译器结构基线

- 入口脚本当前采用模板规则驱动，且触发/事件模板数量较大，已形成可扩展体系，但单文件规模偏大、维护成本持续上升。
- `TemplateRule` 已具备 `priority` 与 `template_metadata` 能力，可作为后续家族化和冲突治理基础。
- 编译流程当前仍保留“先构建语义映射，再重编译产物”的双遍过程。

## 2.2 数据基线（正式 raw）

- 系列目录：`KGD / MCR / MMM / TLR`
- 卡牌总量：442（不含 base sample）
- 触发/效果文本中存在明显高频重复句式（例如抽牌、RAID 触发、AP 激活等），适合收敛为参数化模板族。
- 现状 `Unsupported` 已清零，但后续新增系列仍存在回升风险，需以门禁与统计兜底。

## 2.3 核心问题

1. 模板规模增长快：注册项与 builder 在单文件密集堆叠，新增能力的冲突风险与评审成本上升。
2. 规则复用不足：部分逻辑仍以多段循环/多段正则重复匹配，修改面较大。
3. 可观测性不足：命中统计开关存在，但未作为默认交付数据沉淀。
4. 性能可优化：双遍编译在卡池扩张后会放大编译耗时。

---

## 3. 约束与设计原则

### 3.1 规则一致性约束

- 全程以 `docs/rules/rule.md` 为最高约束，不得以“优化”名义改变结算语义。
- 涉及高风险区（生命触发、攻击/阻挡、AP/能量支付、胜负判定）时，必须先冻结口径再实施。

### 3.2 DSL/IR 约束

- 新增能力必须先抽象为可复用原子要求/步骤。
- 严禁回退到按卡号硬编码。
- “要求”只做可用性/合法性判断；“步骤”只做状态变化执行。

### 3.3 工程约束

- 保持编译输出字段兼容性（`cards_effects.json` / `cards_semantic.json`）。
- 所有阶段至少补一种有效验证（单元、流程、最小对局、冒烟之一）。
- 每一阶段改动同步更新日志与本计划进度。

---

## 4. 分阶段实施路线

## 阶段 P0：可观测性与基线冻结（优先级最高）

### 目标

- 建立“模板命中/回退/冲突”度量基线，形成可追踪决策依据。

### 实施项

1. 模板遥测常态化：
   - 输出每次编译的 family/variant 命中次数
   - 输出 fallback 次数与占比
2. 高风险告警：
   - 同文本命中多规则时记录冲突候选
   - 命中顺序变化时输出差异摘要
3. 基线快照：
   - 将关键指标落盘至 `docs/plan/compiler_metrics_snapshot.json`

### 验收标准

- 能稳定产出 metrics 快照并可对比前一轮。
- 新增模板可通过指标定位“是否误伤旧规则”。

### 进度（2026-04-15）

- 已完成并入 `tools/compile_cards_effects.py` 的模板遥测输出：
  - family / variant 命中次数
  - fallback 次数、分 registry 统计与占比
  - 同文本多命中冲突候选清单（含候选规则与样例卡）
  - 规则命中顺序（rule order）摘要与相对前一轮差异
- `docs/plan/compiler_metrics_snapshot.json` 已升级为阶段 P0 基线格式（含 `cards_metrics` 与 `template_telemetry` 双层口径）。
- 当前基线快照（2026-04-15）：
  - `dispatch_total=2004`，`hits_total=220`，`fallback_total=1784`，`fallback_ratio=0.89022`
  - `conflict_count=1`

---

## 阶段 P1：模板家族参数化收敛（短期主线）

### 目标

- 以 `cards_raw` 高频句式为驱动，减少 exact-by-card 规则与重复 builder。

### 实施项

1. 高频族优先迁移：
   - 抽牌类（含条件抽牌/后续追加）
   - RAID 触发二选一类
   - AP 激活类
2. 统一匹配管线：
   - 将同类“多次遍历 + 多段正则”改为声明式 pattern 列表 + 单次扫描
3. metadata 稳定化：
   - 对同族能力统一 `family` 与 `variant` 命名约定

### 验收标准

- 同族新增文本无需新增按卡分支。
- 同族迁移后语义产物保持一致（步骤序列/要求集合不漂移）。

### 进度（2026-04-15）

- P1 已完成收口，三类高频模板族参数化已稳定接管主路径：
  - `DRAW_SEQUENCE`：固定抽牌文本 `カードをN枚引く。`
  - `LIFE_TRIGGER_RAID_CHOICE`：生命触发二选一文本（`場合/なら` 口径统一）
  - `AP_ACTIVATE`：`AP` 激活文本（纯激活 / 抽牌后激活）
- 已完成统一匹配管线首轮收敛：上述高频族改为声明式共享 pattern 列表生成 trigger/event 规则，减少重复注册与重复正则维护面。
- metadata 口径已冻结：同族统一使用稳定 `family` / `variant` 命名，并由共享规则生成逻辑保证一致性。
- 已新增模板迁移门禁配置与检查脚本：
  - `docs/plan/compiler_metrics_gate.json`
  - `tools/check_compiler_metrics_gate.py`
- 门禁口径（当前）：
  - `fallback_ratio <= 0.9`
  - `conflict_count <= 2`
  - `required_family_hits`：`LIFE_TRIGGER_RAID_CHOICE` / `DRAW_SEQUENCE` / `AP_ACTIVATE` 均需命中

---

## 阶段 P2：模板模块化拆分（中期）

### 目标

- 降低单文件复杂度与并行冲突概率，提升评审效率。

### 实施项

1. 按 family 拆分模板模块（建议）：
   - `preview_*`
   - `bp_*`
   - `raid_*`
   - `cost_modifier_*`
2. 保留统一注册入口与优先级策略，不改变外部调用方式。
3. 引入最小注册一致性检查（重复 name、缺失 metadata、priority 异常）。

### 验收标准

- 主入口仅保留编译装配逻辑。
- 模板新增流程固定为：定义 → 注册 → 回归，不再跨文件散落。

### 进度（2026-04-16）

- 已完成 P2：模板注册一致性检查 + 家族模块化拆分。
- `tools/compile_cards_effects.py` 现已改为“统一入口装配 + 家族模块拼装”模式，并保留统一优先级与外部调用方式不变。
- 新增模板家族模块目录：`tools/card_effects_compiler/template_modules/`
  - `preview_rules.py`
  - `bp_rules.py`
  - `raid_rules.py`
  - `cost_modifier_rules.py`（当前预留空实现，供后续族扩展接入）
- 当前一致性检查项：
  - 重复 `name` 检查（禁止同 registry 内重复规则名）
  - `template_metadata` 完整性检查（必须同时包含 `family` 与 `variant`）
  - `priority` 类型检查（必须为 `int`）
- 已补充回归验证：
  - 编译器单元测试通过
  - 全量编译产物统计保持不变（`Supported=557 / Unsupported=0`）
  - UTF-8 文档校验通过。

---

## 阶段 P3：编译流程性能优化（中期）

### 目标

- 减少全量编译耗时，支持系列持续扩容。

### 实施项

1. 双遍优化：
   - 优先实现中间结果缓存，避免重复构建不可变部分
   - 评估单遍编译 + 延迟语义补全可行性
2. 增量编译：
   - 以 `cards_raw.json` 内容 hash 作为输入签名
   - 未变化系列跳过重编
3. 输出一致性守卫：
   - 增量模式与全量模式结果 diff 校验

### 验收标准

- 增量场景下编译耗时明显下降。
- 全量与增量产物语义一致。

### 进度（2026-04-16）

- 已完成 P3（输入签名 + 中间结果缓存 + 一致性守卫）：
  - 新增系列输入签名：以 `cards_raw.json` 内容 + 模板规则顺序签名共同生成 series 级输入指纹。
  - 新增增量缓存文件：默认写入 `.cache/card_effects_compiler/series_compile_cache.json`，记录每个系列的输入签名与第一遍编译中间结果。
  - 新增第一遍中间结果复用：第二遍编译前可复用缓存的 `first_pass_compiled / first_pass_semantic_entries`，避免重复构建不可变部分。
  - 新增增量跳过：输入签名未变化且 `cards_effects.json / cards_semantic.json` 已存在时，系列级跳过重编并直接复用现有产物聚合指标。
  - CLI 新增控制项：`--incremental-cache`、`--no-incremental`，便于 CI 或排障切换全量模式。
  - 新增一致性守卫脚本：`tools/check_compile_incremental_consistency.py`，默认执行“先全量、再增量”的产物对比，若任一系列 `cards_effects.json / cards_semantic.json` 出现差异则直接失败。
- 已补对应回归测试：
  - 输入签名在 raw 内容变化时必须变化。
  - `_compile_series_cards` 在注入首遍缓存后必须命中 `first_pass_cache_hit`。
  - 新增一致性守卫单测：覆盖 `_diff_snapshots` 的一致、差异、缺失系列三类最小回归。

---

## 阶段 P4：质量门禁与长期治理（持续）

### 目标

- 防止 `Unsupported` 回升和语义退化，形成长期稳定迭代节奏。

### 实施项

1. 门禁项：
   - `Unsupported` 预算门禁（默认 0）
   - 模板冲突门禁（新增冲突阻断）
2. 回归资产：
   - 高频模板族回归用例自动生成（来自 cards_raw 统计）
   - 长链流程样例持续补齐
3. 文档对齐：
   - 每轮更新 `development_plan` 与当日日志

### 验收标准

- PR 阶段可自动发现并阻断模板退化。
- 新系列导入后可快速识别新增缺口并归入模板族路线。

### 进度（2026-04-17）

- 已正式进入 P4，并将本阶段拆分为“模板冲突阻断”与“长期门禁自动化”两条并行子线（在不改变既有 DSL/IR 语义前提下推进）。
- 模板冲突阻断（当前完成）：
  - 冻结冲突口径：继续沿用 P0 已产出的 `conflict_candidates` 作为唯一冲突来源，不新增按卡特判白名单。
  - 冻结阻断策略：PR 门禁默认采用“冲突总量不高于基线 + 高优先级家族无新增冲突”双条件；任一不满足即失败。
  - 冻结回滚策略：冲突门禁失败时，必须先回退模板匹配优先级或 pattern 范围，不允许通过放宽阈值直接放行。
- 长期门禁自动化（当前完成）：
  - 明确 CI 基线输入：`compiler_metrics_snapshot.json` 作为门禁基线源，`compiler_metrics_gate.json` 作为阈值与强制族命中策略源。
  - 明确巡检分层：提交前运行“本地快速门禁”（指标阈值 + 必要 family 命中），合入前运行“全量门禁”（含全量/增量一致性与冲突阻断）。
  - 明确维护职责：新增模板族时必须同步更新 gate 配置与回归样例，避免门禁长期失真。
- 下一步（P4 持续项）：
  1. 增补冲突门禁脚本的“规则顺序漂移 + 冲突联动”联合告警。
  2. 形成按 family 维度的周报快照，跟踪 fallback 与冲突趋势。
  3. 在新系列导入流程中接入自动缺口归类（先 family 归档，再决定是否扩展 IR）。

---

## 5. 任务拆分与里程碑

## M1（对应 P0）

- 交付：
  - 模板命中/回退指标输出
  - metrics 快照更新机制
- DoD：
  - 至少一轮全量编译产出稳定指标
  - 指标变更可被 CI/脚本比较

## M2（对应 P1）

- 交付：
  - 高频三大模板族首批参数化收敛
  - 旧按卡规则迁移清单与替换完成记录
- DoD：
  - 首批迁移后无语义回归
  - 同族新增卡文无需按卡补丁

## M3（对应 P2）

- 交付：
  - 模板模块化拆分
  - 统一注册与静态检查
- DoD：
  - 主入口复杂度下降
  - 模板注册错误可提前失败

## M4（对应 P3）

- 交付：
  - 增量编译 MVP
  - 全量/增量一致性 diff 校验
- DoD：
  - 增量编译可用于本地与 CI
  - 产物一致性可自动验证
- 当前状态：已完成

## M5（对应 P4）

- 交付：
  - 长期门禁策略与自动回归补强
- DoD：
  - `Unsupported` 与模板冲突均纳入稳定门禁
- 当前状态：进行中（2026-04-17 已进入执行）

---

## 6. 验证方案

### 6.1 必跑验证

- `python tools/compile_cards_effects.py`
- `python -m unittest tests/test_compile_cards_effects.py`
- `python tools/check_compile_incremental_consistency.py`
- `python tools/check_utf8_docs.py`（文档改动后）

### 6.2 阶段性验证增强

- 模板冲突扫描脚本
- 产物差异对比脚本（全量 vs 增量）
- 高频模板族专项回归（按 family 维度）

---

## 7. 风险与应对

1. 风险：模板泛化过度导致误匹配  
   应对：提高 priority 治理 + 冲突告警 + 最小样例回归。

2. 风险：拆分模块后注册遗漏  
   应对：引入注册完整性检查与 CI 失败快返。

3. 风险：增量编译造成产物漂移  
   应对：强制全量/增量一致性比对，未通过即回退全量。

4. 风险：新系列文本风格突变  
   应对：保留 fallback 与 unresolved 统计，先归类再扩模板族。

---

## 8. 交付口径

- 本计划为后续 `compile_cards_effects` 的统一实施主文档。
- 若里程碑顺序、DSL/IR 契约、测试策略发生变化，必须先更新本文件与 `docs/plan/development_plan.md`，再进入实现。
- 每轮阶段推进必须同步更新 `docs/logs/log_yyyy-MM-dd.md`（中文）。
