# TcgDemo UI Context

本文件定义 TcgDemo 数字战场 UI 的领域术语。只收录项目特有的 UI 概念；通用 Godot/编程
概念不在此列。

## 语言

### 战场结构

**主面板**（Main Panel）：
玩家战场上的核心区域模块。每个玩家有两个主面板——前线面板和能量线面板。
四个主面板按"敌能量→敌前线→我前线→我能线"四行镜像排列。
主面板有标题条、4 个槽位、`StyleBoxFlat` 半透明深底和圆角。

**前线面板**（Front Line Panel）：
放置角色卡的主面板，标题条使用暖红色系强调。是攻击和阻挡的主战场。
标注 `FRONT LINE · 3/4`（区域名 + 槽位计数）。

**能量线面板**（Energy Line Panel）：
放置角色和场地卡的主面板，标题条使用冷蓝色系。承载能量产能、支援角色和场地。
标注 `ENERGY LINE · 2/4`。

**槽位**（Slot）：
主面板内放置单张卡牌的固定位置。每个主面板有 4 个槽位，横向排列。
空槽以虚线框 + 微弱图案占位，填入卡牌后占位消失。

**标题条**（Title Bar）：
主面板顶部的 28px 高横条。显示区域名、槽位计数（如 `3/4`），右侧挂上下文操作按钮。

### 卫星区域

**附属堆叠**（Attached Stack）：
贴在主面板侧边或角落的紧凑区域显示。以 3-4 张偏移微缩卡背（~40×56px）+ 圆形计数徽标
表达区域内容。点击弹出 `ZoneCardsPopup` 查看完整内容。
_Avoid_: 侧边栏、角落面板、卫星面板

**生命堆叠**（Life Stack）：
附属堆叠的一种，贴在双方前线面板左侧外缘。显示当前生命区剩余卡数。
_Avoid_: LIFE 区域、生命面板

**牌库堆叠**（Deck Stack）：
附属堆叠的一种，贴在双方前线面板右侧外缘。显示主卡组剩余卡数。
_Avoid_: DECK 区域、牌库面板

**场外堆叠 / 移除堆叠**（Outside Stack / Removed Stack）：
附属堆叠的一种，贴在我方能线面板下方角落。分别显示场外和移除区卡数。

### 信息层

**顶部 HUD**（Top HUD）：
屏幕顶部 40px 高的极简信息条。仅显示回合数、阶段名、AP 计数器、能量彩色圆点。
不挂操作按钮（按钮已下沉到面板标题条）。
_Avoid_: 顶栏、状态栏

**预览面板**（Preview Panel）：
屏幕左侧的卡牌预览区。悬停卡牌时显示完整卡面、效果文本和关键词。不跟随鼠标。
_Avoid_: 详情面板、左边栏

**日志抽屉**（Log Drawer）：
从右侧滑入/滑出的可折叠面板。显示对局日志，支持颜色标记、类别筛选、回合折叠。
平时隐藏，只留触发标签。
_Avoid_: 日志面板、右侧栏

### 交互

**上下文按钮**（Context Button）：
挂在主面板标题条右侧的操作按钮，根据当前阶段和游戏状态动态显示。
例如：攻击阶段显示"攻击"按钮，移动阶段显示"移动"按钮。
_Avoid_: 阶段按钮、动作按钮

**面板拖拽**（Panel Drop）：
手牌拖拽到主面板任意位置，系统自动分配第一个空槽位。不需要瞄准特定槽位。
_Avoid_: 槽位拖放

**阻挡高亮**（Block Highlight）：
被攻击方进入阻挡选择时，前线面板边框切金色，可阻挡角色出现脉冲描边。
_Avoid_: 阻挡选择态

**inline 提示**（Inline Hint）：
面板标题条旁闪现的红色错误提示文案，2 秒后淡出。用于目标选择约束违反等场景。
_Avoid_: toast、错误弹窗

### 色板术语

**MD 冷蓝黑底**：
全局背景基底色 `#0A0E17`，深冷蓝黑。来自 Master Duel 参考的赛博竞技场氛围。
_Avoid_: 桌垫红、暖黑底

**青强调**：
主交互强调色 `#00ACC1`，用于选中态、高亮描边、前线面板标题条。
_Avoid_: 主强调红

**暗金高亮**：
资源/重点提示色 `#D4A843`，用于 AP 计数器、关键激活态、选中面板边框。

## 示例对话

> **Dev**：一个回合里，玩家先点了手牌里的角色卡，拖到前线面板松手——这一套操作叫什么？
>
> **Domain**：那是"面板拖拽"到前线面板。落点是整个面板，系统自动填第一个空槽。
> 出牌成功后，那个槽位从虚线框变成卡面，标题条的计数从 `2/4` 变成 `3/4`。
>
> **Dev**：能量线面板也走同样的拖拽逻辑？
>
> **Domain**：对，也是面板拖拽。拖到哪个面板就出到哪个面板。如果你拖手牌到
> 前线面板但它满了（`4/4`），系统会在标题条闪一条 inline 提示"前线已满"。
>
> **Dev**：我如果想看生命区还剩几张呢？
>
> **Domain**：看前线面板左侧外缘的生命堆叠——3-4 张偏移的微缩卡背，右上角
> 圆形徽标写 `×5`。点击堆叠弹出 ZoneCardsPopup，能看到全部生命卡。
